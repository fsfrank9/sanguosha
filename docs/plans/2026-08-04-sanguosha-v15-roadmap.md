# v15 方向: 三扩展包内容接入 + 蛊惑收尾 + 清账收官

**日期**: 2026-08-04
**起点**: v14 收官 (O #183 / P #184 / Q #185 / R-A #186 / R-B #187 全部合并)
**前置状态**: 武将 71 名 / 技能槽位 128 (唯一 ID 123) / 已接入 54 技
(35 将全实现 + 5 将部分实现, 未实现 69 技); 测试 191 文件全绿; 常驻门禁
v12_i 63.5% (≥55%) / M4 推断 5p 67.4% · 4p 69.1% / Q2 内奸基准;
快档 14 文件 (~2s); 四轮全量合规审计在案 (最近 audit4 2026-07-19)。

## 缘起

v14 收官后全量盘点 (候选账本 / 内容缺口 / 工程债 三路并查) 得出两个
关键事实, 直接决定 v15 主线:

**事实一 (纠正 v13 N3 判定)**: v13 N3 (2026-07-18) 曾判 "火/林/山在
缓存中无任何专门 spec 源 (仅规则站武将页旁涉提及), 按 spec 红线零接
入"。本次逐条复核确认该结论**与仓库实况不符**: gltjk 镜像按势力分页
(`card__hero__{wei,shu,wu,neutral}.md`), 每页内以官方 "技能名——完整
规则文本" 格式完整收录各扩展包武将 (行内 (火)/(林)/(山) 标签区分),
林 18 技 / 火 13 技 / 山 17 技 **48 项未实现技能的逐字文本 100% 已在
本地缓存** (48 条 文件:行 逐条复核在案, 如 八阵/火计/看破
`card__hero__shu.md:336/338/340`、化身/新生
`card__hero__neutral.md:249/266`)。红线原文是 "有可靠来源再立项" —
来源已证实存在, 立项条件满足。N3 判定按 "事实误判" 更正, 非重议用户
裁定。

**事实二**: 未实现 69 技中, 48 技 (林/火/山) + 3 技 (SP 关羽单骑/SP
袁术庸肆·伪帝) 本地有逐字源可直接立项; 其余 18 技 (SP 10 将) 本地零
命中, 采集渠道 (www.sanguosha.com/hero) 存在但本环境网络拦截
(CONNECT 403, O4 已裁定网络变更触发回填), 归 "条件性候选" 非红线。

### 候选来源表

| 候选 | 来源记录 | 已预留基建 |
|------|---------|-----------|
| 蛊惑响应窗口打出流程 | v14 R1 已知局限① (roadmap:551-553) | guhuo.js 质疑链/翻验三态全套; findResponseCard 打出接缝 |
| 蛊惑五型富交互声明 UI | v14 R1 交付说明 (roadmap:548-550) | 引擎 API 已支持全 16 型; mode-panels 区域/成本/双段流程齐备 |
| 蛊惑 AI 全型声明启发 | v14 R1 评审收口 [记录不修]① | aiShouldChallengeGuhuo 三面质疑已扩; aiMaybeDeclareGuhuo 单型 v1 |
| 林包 8 将 18 技 | 本次盘点 A 类 (逐字源在库) | 完杀 (glossary__flow.md 判例已引用)/乱武 (蛊惑判例已引用) 等与既有机制互文 |
| 火包 8 将 13 技 | 本次盘点 A 类 (逐字源在库) | 火杀/藤甲/铁索连环火伤链 v13-N 已入引擎 |
| 山包 8 将 17 技 | 本次盘点 A 类 (逐字源在库) | 观星式看牌重排/翻面 (据守) 等先例 |
| SP 单骑/庸肆/伪帝 3 技 | 本次盘点 A 类 (逐字源在库) | 主公身份判定 (danji 依赖 "主公为曹操") 先例 |
| AI 反间挂起缺 ok 字段 | v14 P4 滚动候选 (roadmap:301-302, 唯一显式滚动未回收缺陷) | 1200 种子 fuzz 复现面在案 |
| renderPendingChoice 表驱动重构 | 2026-06-09 审计 backlog:76 (前置条件已具备) | PENDING_MODAL_DISPATCH 注册表先例 |
| 沉默残留清账 (克己/寒冰可选、randomSuit Math.random 兜底、M9 不可测备案、jsdom 账本) | 2026-06-09 审计 backlog:75,81,83 + audit4:70-71 (无声漂移, 需显式裁决) | — |
| 第五轮全量合规审计 | 惯例 (audit4 距今 14 天暂不独立排; 三包 48 技接入后重新成立) | 12 域并行审计方法论 (audit4) |

**既定口径, 不再重议** (v14 缘起同款清单全量继承 + v14 新增):
6 人以上大场收案 (5 人封顶) / ES5 var 转换不做 / KOF 不立项占位保留
(零死钮验收的用户裁定例外, 不得清理不得主动重估) / 战报回放·移动端
触屏未点单收案 / 国际化终止滚动 / 出牌阶段桃仅对自己 / 杀·无双双闪
deferred-ask 口径 / 护驾候选身份过滤设计 / M2 零直读白名单 4 处维持 /
军争花色点数分布维度永久不可验证 (无官方数据表)。

**排序依赖观察**:
1. 蛊惑打出流程 (S1) 是响应窗口横切面, 先行落地可为林包乱武 (贾诩)
   的 "乱武×蛊惑响应" 官方判例面提供完整互测场 — S 在 U 前。
2. 火包体量最小 (13 技) 且火伤链基建最全, 作三包首批验证 "势力页
   spec 提取 → 版本基准裁定 → 接入三件套" 的批内流水线; 林继之
   (乱武/完杀与既有蛊惑/濒死机制互文最深), 山殿后 (左慈化身为全
   目录最复杂单技, 允许独立拆批或按成本评估门裁剪)。
3. 清账收官 (W) 吸收全部散账 + 第五轮审计, 放在内容批全部落地后 —
   审计对象含新入 48 技, 时点才成立。

**结论排序**: S 蛊惑收尾 → T 火包 → U 林包 → V 山包 → W 清账收官。

## 总体策略

- 沿用 v12-v14 全程验证的工作规范: 每批独立 PR、`npm run verify`
  全档门禁、行为测试三件套 (引擎+UI+守护)、后台 merge 驱动、推送前
  核对上游 PR 状态、CLAUDE.md subagent 模型分配。
- **方法论增补 (v14 教训固化)**: ① 每批评审收口必含 opus 对抗端到端
  复现 (v14 R-B 牌面伪造级缺陷即由此抓出); ② 新增 UI 测试一律用
  ui-game.mjs 重掷 helper 或显式身份, 禁自写随机开局 (R-B flaky 教
  训); ③ 涉及官方文本的接入批, spec 提取简报随批入库 docs/ (R1 调研
  简报未入库教训)。
- **技能接入批内流水线** (T/U/V 通用): 势力页逐字提取 → 版本基准
  裁定 (标/1V1/3V3/国-标 变体按 1v1+身份场既有口径, 分歧记档如蛊惑
  先例) → 引擎+UI+AI 启发 → 行为测试 → fixture/缓存/图鉴翻转。
  新包 fixture 沿用 wind 样板 (`containsOfficialText:false` +
  sourceTextRef 摘要 + gid 临时编号策略)。
- **红线**: 既有 54 技全部行为测试零改动通过; 三基准门禁不降
  (数字漂移按 "重测回钉" 惯例); 守恒/架构红线快档恒绿。

## 阶段拆分

### S. 蛊惑收尾批 (P0)

- **S1 响应窗口打出流程**: 官方 ◆ 打出流程落地 — 需要打出 杀/闪/桃/
  无懈 的响应窗口 (决斗/南蛮/万箭/借刀应杀, 杀应闪, 濒死应桃, 锦囊
  应无懈) 挂蛊惑声明入口; 质疑链复用, 验假 "视为没有决定如何进行
  响应" (响应窗口重开, 可再蛊惑 — 乱武判例 `card__hero__neutral.md:334`
  逐字口径); 打出流程无目标合法性步骤。R1 存档的 8 项 spec 缺口逐条
  复核 (打出◆缠怨授予措辞不对称等), 裁定入库。
- **S2 五型富交互声明 UI + AI 全型演进**: 拆/顺/火攻/借刀/铁索 声明
  菜单补齐 (区域/成本/双段目标复用既有 mode-panels 流程, 菜单
  11→16); AI 声明启发从无中单型扩全型 (真牌恒蛊惑 + 按型 EV 诈声明,
  与三面质疑启发自洽), 质疑启发增 "被抓包记忆" (stanceLog 式公开
  记账, 不读暗牌)。验收: 蛊惑判例面测试从使用流程扩到打出流程。

### T. 火包接入批 (P0)

- 8 将 13 技: 典韦强袭 / 荀彧驱虎·节命 / 卧龙八阵·火计·看破 / 庞统
  连环·涅槃 / 太史慈天义 / 庞德猛进 / 颜良文丑双雄 / 袁绍乱击·血裔。
- 与既有基建互文: 八阵=八卦阵同判定复用; 连环=铁索连环转移; 涅槃=
  濒死框架 (不屈先例); 乱击=万箭虚拟牌 (card-as 先例); 血裔=主公
  身份判定 (识别群势力存活数)。批内按 "使用系→触发系→濒死系" 小批
  推进, 允许 2 批拆分。

### U. 林包接入批 (P1)

- 8 将 18 技: 徐晃断粮 / 曹丕行殇·放逐·颂威 / 孙坚英魂 / 鲁肃好施·
  缔盟 / 孟获祸首·再起 / 祝融巨象·烈刃 / 贾诩完杀·乱武·帷幕 / 董卓
  酒池·肉林·崩坏·暴虐。
- 完杀 (濒死救援限制) 与乱武 (全场逐席应杀) 均已有官方判例在既有
  测试引用中出现 (glossary__flow.md:91 完杀×蛊惑, 依赖 S1); 帷幕=
  锦囊目标免疫 (谦逊先例); 断粮=兵粮寸断 card-as。主公技 (颂威/
  暴虐) 随身份场激活惯例。允许 2-3 批拆分。

### V. 山包接入批 (P1, 末项决策门)

- 8 将 17 技: 张郃巧变 / 邓艾屯田·凿险 / 刘禅享乐·放权·若愚 / 姜维
  挑衅·志继 / 孙策激昂·魂姿·制霸 / 张昭张纮直谏·固政 / 蔡文姬悲歌·
  断肠 / 左慈化身·新生。
- 武将牌上置牌区 ("田"/"幻") 为新区域面 — 守恒 census 扩展先行;
  享乐=乐不思蜀判定变体; 激昂 v14 P3 判例已引用。**左慈化身为全目录
  最复杂单技 (获得并切换其他武将技能), 独立成本评估门**: 估算超出
  单批容量则按 "宁缺毋滥" 记录候选不强行接入, 其余 7 将不受连累。
  允许 2-3 批拆分。

### W. 清账收官批 (P2)

- **W1 散账清零**: AI 反间挂起缺 ok 字段修复 (v14 P4 滚动候选回收);
  renderPendingChoice 表驱动重构 (前置条件已具备); 沉默残留显式裁决
  — 克己/寒冰可选、randomSuit Math.random 兜底、M9 不可测备案、
  jsdom 账本 (拟按 "fake-dom + Playwright 双层已等效覆盖" 正式收案,
  维持零依赖红线) — 逐项 做/收案 二选一, 不许继续无声漂移。
- **W2 第五轮全量合规审计**: 对象 = 接入后全量 ~102 技 + 全牌型,
  12 域并行方法论 (audit4 样板); SP 18 技 spec 采集保持 "网络环境
  变更触发" 条款 (gid 回填同款), 不主动重试。
- README/history.md v15 编年收官。

## 验收标准

- S 后: 蛊惑打出流程判例面测试落地 (乱武响应/濒死蛊惑桃至少各一
  钉), 声明菜单 16/16 型, AI 全型启发带自洽性测试; 既有蛊惑 22 例
  零改动通过。
- T/U/V 后: 各包全部技能三件套 (引擎+UI+行为测试) + fixture/缓存/
  图鉴翻转 + 图鉴零 "看起来有但触发不了"; 每批全量 verify 绿 + 三
  基准门禁不降; 新区域面 (屯田/化身) 守恒 census 覆盖; 左慈门若裁
  剪则如实记录候选。
- W 后: 散账清单逐项 "已做/已收案" 二态归零, 无 "无声漂移" 残留;
  第五轮审计发现→确证→修复闭环入 docs/audit/。
- 全程: 零 npm 依赖、每批独立 PR、推送前核对上游 PR、方法论三区
  自查入每批门禁。

## 非目标

- 不做联网对战 / 房间系统 (本地同屏 + AI 座席不变)。
- 不引入打包器 / 框架 / jsdom (零依赖红线不变; jsdom 账本经 W1 显式
  收案)。
- 不做 6 人以上大场 (5 人封顶, v14 终态收案不重议)。
- 不重写 UI 视觉体系。
- KOF / 炼狱 KOF 占位卡不清理、不主动重估 (用户裁定例外维持)。
- 战报回放/撤销、移动端触屏、国际化 — 未点单收案维持, 用户重新点单
  才复活。
- SP 10 将 18 技不主动排期 (本地无逐字源; www.sanguosha.com 采集渠道
  网络受限, 保留 "网络环境变更触发回填" 条款)。
- ES5 var 全量转换不做 (v14 终态)。
- 神/西/非游戏势力武将 (gltjk 镜像有文本) 不入目录 — 目录边界维持
  71 将, 扩目录需用户点单。

## 排期与节奏

按 v13/v14 实测节奏 (每批一个 PR、后台 merge 驱动), 合计约 8-12 批:
（S 已按用户点单合并为一批交付, 见下方执行记录。）
S 2 → T 1-2 → U 2-3 → V 2-3 (左慈门可独立/裁剪) → W 1-2。顺序执行,
阶段内小批推进; 技能接入批若单将复杂度失控 (化身级), 允许按将拆批
以批次数换确定性。每阶段末盘点批同时是 "是否继续下一阶段" 的决策点;
V 左慈成本评估门与 W1 散账逐项裁决在 S-U 期间即可并行征询用户, 不
阻塞主线。

---

## S 执行记录 (2026-08-05, 一批交付 S1+S2)

**范围**: 用户点单"一次性完成 S 阶段" → S1 (响应窗口打出流程) 与 S2
(声明菜单 16/16 + AI 全型启发) 同批交付。两者不可拆: S1 的"验假重开"
判例需要 S2 新增的「响应赌假」质疑面才能端到端复现 (否则 AI 对响应中
的声明恒不质疑), 拆开会留下非绿中间态。

### S1 响应窗口【打出流程】/【响应中的使用流程】

- **表驱动八窗口** (`guhuo.js GUHUO_RESPONSE_WINDOWS`): 闪应 杀/万箭/
  银月 (`shan-response`/`wanjian-response`/`yinyue-response`)、杀应决斗
  (`sha-duel-response`, 三属性分列)、杀应南蛮 (`aoe-sha-response`, 评审
  收口新增)、杀应借刀 (`jiedao-decision`, 评审收口放宽)、桃·酒应濒死
  (`dying-rescue`, 酒仅自救)、无懈应锦囊 (`wuxie-response`)。可声明牌名
  由引擎按窗口给出, UI 不自行推断。
- **声明期只定牌名**: 打出流程无"选择目标时"步骤、无"取消不合法的
  目标" (官方 `card__hero__neutral.md:336` 逐字), 目标由窗口自身确定;
  质疑链、亮出、缠怨与使用流程复用同一实现。
- **验假 = 窗口原样重开**: "视为你没有决定如何进行响应" — 原
  pendingChoice 对象回槽, 该窗口的 pauseState 未被触碰; 本回合限次已
  消耗, 重开窗内不能再蛊惑 (缘起表述"可再蛊惑"按 G4 更正)。
- **验真/无质疑 = 注入回原流程**: `pauseState.guhuoResponse` 携实体牌
  + 声明牌名, 由 `consumeResponse` / `consumeWuxie` / 濒死救援三个出口
  取用 (一次性消费, 未消费则退回手牌兜底)。实体牌面即最终牌面 → 银月
  枪等"回合外黑牌"时机照常按实体牌面派发。
- **开窗门槛放宽**: 于吉可背面朝上打出任意手牌 → 手上没有所需牌型也
  必须开窗 (此前"无闪不开窗/无懈不开窗"把蛊惑的响应面直接锁死)。六处
  gate 统一走 `guhuoResponsePossible` 谓词 (声明入口本身亦复用同一谓词),
  仅玩家席。
- **限次修正 (R1 遗留缺陷)**: "每名角色的回合内限一次"是按回合刷新的
  全场额度 (`rule__principle.md:58` 判例: 于吉在贾诩的回合内发动蛊惑)。
  原 `resetActorTurnState` 只复位回合主 → 响应面接入后额度会永久卡死;
  改由 `phases.resetGuhuoTurnLimit` 在每个回合开始清全场。
- **拦截口**: `resolvePendingChoice` / `resolveResponseChoice` 双
  dispatcher 在 `decision.guhuo` 时前置拦截 (声明是"如何响应"的另一条
  分支, 不是该 kind 的一个决策值); 校验失败窗口原样留槽, 零副作用。
- **UI**: `guhuoResponsePanel` 与所在响应窗口**并列**显示 (选牌名 +
  盖置手牌 + 确认门禁)。刻意不进 `PENDING_MODAL_DISPATCH` — hand-confirm
  快捷键留给主窗口的"打出真牌", 蛊惑走面板内自己的确认按钮。

### S2 声明菜单 16/16 + AI 全型启发

- **菜单补齐 11→16** (`mode-panels.js GUHUO_UI_TYPES`): 拆/顺/火攻 转
  座席点选 (区域·具体牌·火攻成本仍由结算期既有 pendingChoice 自选, 不
  进声明面); 借刀两段点选 (受害者候选唯一时直接成局 — 与既有借刀出牌
  流 `resolveSeatPick` 同款); 铁索 1-2 名座席 (仅"使用"分支, 重铸不是
  使用/打出, 无入口)。
- **AI 声明启发扩全型** (`aiMaybeDeclareGuhuo`): ① 真牌恒蛊惑按
  `GUHUO_DECLARE_PRIORITY` EV 序逐型试 (无人质疑等价直出, 被质疑反赚
  缠怨 → 严格不劣); ② 无可用真牌时用死牌 (闪/无懈) 按同序诈, 预判有
  质疑者即抑制。合法性一律交 `playGuhuoDeclare` 判 (声明失败零副作用),
  AI 侧只排序与挑目标 — 规则单一真相源, 不复制。
- **质疑面收官** (`aiShouldChallengeGuhuo`, 原三面 → 五面): ④ 响应
  赌假 (声明是对"我推进中的事件"的响应: 闪应我的杀/万箭/银月, 杀应我
  发起的决斗, 桃救我正打死的人) 且 hp>=2; ⑤ 被抓包记忆 (亮出结果按
  声明者记公开账 `guhuoBusted`/`guhuoProven`, 前者多于后者且敌对且
  hp>=2 → 质疑)。两面均只读公开信息, 不读暗牌不读暗置身份。

### spec 缺口裁定

12 条逐条裁定入库 `docs/audit/2026-08-05-guhuo-spec-gaps.md` (方法论
增补③兑现 — R1 简报未入库的教训)。其中三条改变了实现:
G1 (打出◆缠怨措辞不对称 → 授予)、G4 (验假重开不可再蛊惑)、G6 (限次
是按回合刷新的全场额度 → 修 R1 遗留缺陷)。G12 为前瞻记录: 林包【乱武】
是"响应中的使用流程且需选目标"的窗口 (`flow__use.md:39` 判例), U 阶段
接入时需为响应声明补目标步骤。

### 评审收口 (2026-08-05, 同批 — opus 对抗端到端复现)

方法论增补① 兑现。评审在 scratchpad 内实跑 100 局自对弈 (逐步 census)
+ 八个专项复现脚本, 报 4 中 / 2 低 / 1 存疑, **4 条中级全部修复**, 其余
逐条裁定入档:

- **[中] 同批内部不对称漏补**: 决斗【无双】第二张【杀】的窗口没跟着放宽
  (`tricks.js`), 于吉手里有牌、额度未消耗却被直接判"无法打出第二张"。
  修: 与 sha-flow 的第二张【闪】窗口对称补 `guhuoResponsePossible`。
- **[中] 边界门禁缺失**: `declareGuhuoResponse` 没复用 `guhuoResponsePossible`,
  AI 席经 `skillPreferences.dying='ask'` 拿到窗口后可从公开 dispatcher
  直调发动 — 正好落进本批明文声明**不支持**的区域。修: 声明入口复用
  同一谓词; `guhuoResponseTypes` 导出面改走 `guhuoResponseMenu`, 与
  `guhuoResponseAvailable` 同口径 (两个导出谓词不得互相矛盾)。
- **[中] UI 零确认发动**: 面板本地态按 **kind 字符串**比较, 同 kind 连开
  的两个窗口 (无双第二张闪 / AOE 逐席) 之间暂存原样残留且确认钮已启用,
  单击即消耗额度并丢牌 — 违反"最终生效必过确认"。修: 改认 pending
  **对象**身份。
- **[中] AI 「响应赌假」无条件恒质疑**: 于吉用**真牌**声明即可零成本钓走
  缠怨, 此后 1v1 质疑队列恒空、全 16 型可无风险诈。修: "挡住我"降为
  必要条件, 另需三条独立赌假理由之一 (有前科 / 响应空窗记账 /
  高赌注: 濒死救援或声明者血线见底), 且质疑者 hp>=2。
- **[低→改为实现] 南蛮·借刀应杀无玩家窗口**: 评审建议记为局限, **不采纳**
  —— 路线图 S1 明文含"南蛮/借刀应杀", 属漏做而非局限。本批补齐:
  南蛮新增 `aoe-sha-response` 玩家窗口 (仅在玩家可发动蛊惑时开, 其余
  局面维持既有自动响应, 零行为变更); 借刀的 `jiedao-decision` 窗口
  gate 放宽 + `jiedaoFireOpponentSha` 接注入口。两窗入表 →
  **响应窗口 6 → 8**。
- **[低] 质疑资格用 `hp > 0`** 与 **[存疑] 翻面跳过的回合不刷新额度**:
  各自裁定入 spec 简报 G13/G14 (维持现状, 附理由)。
- 评审同时确证: 100 局逐步 census **零** ID 增减/跨区重复; v14 R1 那类
  "声明型门面改写实体牌面"未复发; 注入口的 actor+type 双匹配未能被
  跨座席/跨牌型消费; 验假重开后各窗 pauseState 自洽 (含无双
  `shanRemaining` 保持); AI 逐型试零副作用; 无信息泄露。

### 测试与门禁

- `tests/v15_s1_guhuo_response.test.mjs` 引擎 20 例 (含 5 条评审收口钉:
  南蛮开窗/南蛮零行为变更面/借刀决定窗/无双第二张杀窗/AI 席越界拒绝) (六窗口开窗/三态/
  验假重开/濒死桃判例/守恒/限次/牌面不被改写/声明门零副作用/AI 席局限
  钉); `tests/v15_s2_guhuo_ai.test.mjs` 14 例 (全型声明/EV 序跳过/诈声明
  抑制/AI 生态自洽/响应赌假四面逐条/被抓包记忆三态); `tests/ui_v15_s_guhuo.test.mjs`
  UI 11 例 (响应面板门禁/验假重开后入口收起/濒死窗/非于吉零入口/同 kind
  连开窗口零脏值/菜单 16/16/顺手·铁索·借刀 1v1 与 3p 点选链)。
- 既有 `v14_r1_guhuo` 16 例 + `ui_v14_r1_guhuo` 6 例**零改动**通过 (红线)。
- 全档 194 文件绿; 三基准门禁不降: v12_i 63.5% (≥55%)、M4 5p 67.4% /
  4p 69.1%、Q2 内奸分布可测移动。UI 测试 5 连跑零失败 (R-B flaky 教训:
  重掷至玩家先手 + 显式置回合)。

### 方法论三区自查

① **信息**: 质疑窗与日志在亮出前只给声明牌名与目标 (公开面), 不泄露
盖置牌; AI 两个新增面只读公开信息 (声明型/目标/自身状态/感知敌对/
已亮出过的牌)。② **术语**: 「打出流程」「视为没有决定如何进行响应」
「确定除牌名外的所有牌面信息」逐字对照 `:336`。③ **时机**: 质疑窗固定
在声明后·亮出前, 环序从当前回合角色起 (`rule__principle.md:50`), 首
质疑即止; 限次按回合刷新。

### 已知局限 (如实记录)

- AI 席在响应窗口不声明蛊惑 (仅玩家席)。需把全部响应消费出口改造成
  可挂起续跑 (杀链/AOE 逐席/决斗链/濒死链/无懈链), 属独立工程批。
  边界由 `guhuoResponsePossible` 单点把守 (评审收口后声明入口亦过闸),
  回归钉「AI 席不得经公开 dispatcher 越界发动」。
- 林包【乱武】式"响应中的使用流程且需选目标"窗口留待 U (G12)。
- 质疑资格沿用全仓 `hp > 0` 存活判据 → 濒死角色 (hp≤0 但未阵亡) 本轮
  不能质疑 (G13, 维持现状)。
- 翻面/阵亡跳过的回合不刷新蛊惑全场额度 (G14, 按"跳过回合 = 没有回合
  开始"处理)。

---

## T 执行记录 (2026-08-05, 火包 8 将 13 技 + 拼点框架)

**范围**: 用户点单"一次完成 T 阶段" → 火包 13 技一批交付 (路线图原允许
2 批拆分)。前置勘查用 7 个 subagent 并行跑 (6 组逐技能 spec/接缝 +
1 组 opus 拼点架构), 按 CLAUDE.md 模型分配 (勘查 sonnet, 架构 opus)。

### 新增基建: 拼点 (src/engine/pindian.js)

全新机制, 引擎此前零实现。按 `flow__rankcompare.md` 逐字落地:
发起者选目标 → 双方各扣置一张手牌入处理区 → 亮出比点数 (**点数相同则
两名角色都没赢**, 发起者按没赢结算) → "赢/没赢后"效果 → 处理区拼点牌
入弃牌堆。

- **顺序红线**: 弃置在效果**之后** — `flushPindianCards` 只弃置"结算后
  仍不在任何区域"的拼点牌, 为林包【烈刃】"获得其拼点牌"预留认领面。
  若图省事提前弃置, U 阶段接烈刃时必踩。
- **挂起模型**: 玩家席选牌经 `pendingChoice 'pindian-card'`, AI 席同步
  选; 双方都是 AI 席时整个拼点同步跑完。选牌期实体牌锚在
  `pauseState.pindian` (守恒 census 深扫在途面)。
- **效果注册表**: `registerPindianContinuation(key, handler)` — 拼点
  框架不认识任何技能 (无懈链 continuation 同款), 后续 烈刃/制霸/间书
  直接挂。
- **点数数值化**: `CardRuntime.cardRankValue` 单点出口 (A=1 … K=13)。

### 13 技接入要点

| 技能 | 落点 |
|------|------|
| 强袭 | 主动技; 成本二选一 (失 1 体力 / 弃武器, 手牌与装备区皆可); **弃装备武器时按 `glossary__card.md:41` 不能再用该武器的攻击范围** |
| 驱虎 | 拼点; 赢 = 拼点目标对其攻击范围内由荀彧选的角色造成 1 伤害 (伤害来源恒为拼点目标), 没赢 = 拼点目标对荀彧造成 1 伤害 |
| 节命 | onDamageAfter 逐点触发, 令一名角色补手牌至 min(体力上限, 5) |
| 八阵 | `state.js` 新增**虚拟装备层** — "视为装备着某装备牌"挂在 `hasEquipmentEffect` 单点上, 对既有装备面自动一致 |
| 火计 | 红色**手牌**当火攻 (限手牌, 对照武圣的"红色牌"含装备区) |
| 看破 | 无懈候选/开窗门槛/消费三处共用 `wuxieOptionForCard` 谓词 |
| 连环 | 梅花手牌当铁索 + **重铸面** (新增 `canRecastCard`/`recastHandCard` 统一入口与 `onCanRecast` hook) |
| 涅槃 | 限定技 (`flags.niepanUsed` 永不复位); onDyingEnter 弃区域所有牌 → 武将牌复原 → 摸三 → 体力回复至 3 |
| 天义 | 拼点; 赢 = 杀额外次数 +1 / 无距离限制 / 额外目标 +1, 没赢 = 本回合不能使用杀。次数与禁用闸收敛到 `StateRuntime.shaUseAllowed` |
| 猛进 | 新增 `onShaDodged` hook (位序: 贯石斧之后、青龙续杀之前); 玩家席挂 `mengjin-pick` 并由 resolver 续跑闪避分支剩余流程 |
| 双雄 | 摸牌阶段改判定 + 经 `claimed` 通道获得判定牌 (天妒同一单点), 回合级授权异色手牌当决斗 |
| 乱击 | 两张同花色手牌合成虚拟【万箭齐发】(丈八双牌虚拟先例) |
| 血裔 | 主公技锁定技, `handLimit` +2X (X = 存活的其他群势力角色数) |

### 架构面改动 (跨技能)

- `canPlayCardAs` 白名单改由**转化牌工厂表**驱动, 锦囊分支统一交给已
  注册的结算 handler (无懈链/逐目标/成本询问全部复用), 不再逐型手写 —
  新增可转化牌名只改一处。既有 杀/乐/拆 三条路径逐字等价。
- `resolveShaAfterResponse` 拆成 `continueShaDodgeAfterSkills` +
  `settleShaHit` 两个入口 (猛进 ask 挂起后的续跑面)。
- 数据面: IMPLEMENTED 54→67 / ACTIVE 12→16; SKILL_METADATA 13 条, 新增
  三个规范值 (trigger `shaDodged` / frequency `oncePerGame` /
  cost `rankCompare`); 火包 cache+specs+skills 三份 fixture。
  **sourceTextRef 取已入库 gltjk 镜像行的 sha256** — 与风包"人工转写、
  无法复核"不同, 火包每条都可逐条复核。

### spec 缺口裁定

13 条入库 `docs/audit/2026-08-05-fire-pack-spec.md`。其中三条来自并行
勘查的发现并**改变了实现**: F11 (弃武器作成本后不能用该武器的攻击范围)、
F12 (成本武器可来自手牌)、F13 ("攻击范围内的角色"不含自己 — 由
`glossary__value.md:114` 的"所有**其他**角色"与 `rule__classification.md:69`
"存在攻击范围内没有角色的角色"两条互证)。

勘查报告另有一条建议被**驳回**: 有 agent 依"自己与自己的距离永远是 0"
推断 P 在自己攻击范围内、驱虎可令 P 自伤。该推断与它自己引用的
`rule__classification.md:69` 冲突 (若自己恒在自己范围内, "攻击范围内
没有角色"不可能成立), 按 F13 裁定不采纳。

### 测试与门禁

- `tests/v15_t_fire_pack.test.mjs` 30 例 (13 技逐条 + 门槛 + 守恒 +
  官方判例钉); `tests/v15_t_pindian.test.mjs` 8 例 (三态/点数序/门槛/
  挂起在途/兜底/全 AI 同步/AI 出牌启发); `tests/ui_v15_t_fire_pack.test.mjs`
  8 例 (座席点选/拼点面板/猛进面板/重铸入口/乱击选牌/图鉴翻转)。
- `tests/v15_t_review_closure.test.mjs` 22 例 (评审报告的每条已修项各一钉)
  + `ui_v15_t_fire_pack` 追加 4 例 (M3 背靠背窗口 / 四个新面板锚点 /
  双雄面板 / 涅槃面板)。
- 全档 198 文件绿; 三基准门禁不降 (v12_i 64.0% ≥55% / M4 5p 67.9%·4p 69.1% /
  Q2 分布可测移动); UI 测试 3 连跑零失败。
- 守护测试同步上调: 感知路由计数 3→6、todo 下限 64→50、phase flags 快照。

### 评审收口 (opus 对抗端到端复现, 方法论增补①)

200 局火包全将 soak + 12 个专项复现脚本, 报 **1 高 / 10 中 / 6 低 / 2 存疑**,
高与中**全修**, 低级 5 条一并修 (L4 点选上限也修), 存疑 2 条一条转实做、
一条如实归档。新增 `tests/v15_t_review_closure.test.mjs` 22 条回归钉
(编号与报告一一对应)。

**[高] H1 — 猛进接线的早退吞掉整条闪避分支, 【杀】永久离场。**
`resolveShaAfterResponse` 在 `onShaDodged` 之后无差别检查 `game.pendingChoice`,
但闪响应本身就可能已挂起一个与猛进无关的窗口 (`consumeResponse` 派发
`onShanUsed` → 张角雷击对玩家席开 `leiji-ask`)。于是**局内没有庞德也会早退**,
而 `pauseState.shaDodgeResume` 只有 `resolveMengjinPickChoice` 认 → 该【杀】
永不 `settleShaCardAfterOutcome`, 从所有区域消失, 青龙续杀被静默吞掉。
守恒 census 因深扫 `pauseState` 反而"通过", 现有守护测试抓不到。
判据改为**钩子自身的挂起信号** (`suspendedForMengjin`)。同一教训随后用在
涅槃的 `onDyingEnter` 上。

**[中] 10 条**

| # | 缺陷 | 收口 |
|---|------|------|
| M1 | 丈八/激将/AI 评分三处出杀闸未迁 `shaUseAllowed` → 天义两个方向都错 | 三处统一走单点 |
| M2 | 天义"无距离限制 / 额外目标 +1"只落在结算层, 目标枚举面没接 → UI/AI 只拿得到"额外次数 +1" | 新增 `shaUseReachAllowed` (F14) 接进 `isLegalCardTarget`; `shaExtraTargetLimit` 与 `playCardAs` 前置检查改用与结算层同一算式 |
| M3 | UI staged 槽只在窗口全空时清 → 同 kind 背靠背窗口间残留暂存, 第二窗按"确定"即零确认执行上一窗选择 | 绑定 `pendingChoice` 对象身份 (S 批蛊惑面板同款教训, 这次修在共用槽上, 后续新面板天然免疫) |
| M4 | `recastHandCard` 缺 `pendingChoiceGuard` (UI 重铸钮直调该函数) | 补守卫 |
| M5 | `playCardAs` 守恒兜底被 id 语义击穿 — handler 拒绝路径退回的是虚拟牌 (与来源同 id) → 手牌实体被永久改写成另一张牌 | 新增 `CardRuntime.findCardZoneByRef` / `removeCardRefFromZones`, 转化杀与转化锦囊两条兜底都改按对象身份 |
| M6 | 四个官方"你可以 / 你选择"被实现成自动 (F17) | 涅槃/双雄/驱虎/节命 各接一个 pendingChoice 窗 + UI 面板 |
| M7 | 看破的自动路径按手牌顺序取第一张黑牌 → 一张【青釭剑】被拿去抵消【过河拆桥】 | 在合格候选里按 AI 弃牌估值取最不值钱的一张 |
| M8 | AI 强袭启发按当前武器射程选目标, 引擎按 F11 的 `effectiveRange` 校验 → 目标被拒, **整段出牌阶段中断** (200 局 soak 命中 5 次) | 启发按成本档分别算射程 (弃武器档 ≤1 / 失体力档按武器) |
| M9 | 涅槃在判定阶段弃不到判定区的牌 (整批已 splice 成在途) → 被"弃置"的延时锦囊照常生效 | 新增 `pauseState.judgeAreaInFlight` 活引用, 区域清空类效果可认领在途批次, 判定循环跳过已认领项 |
| M10 | 强袭失体力致濒死时, 伤害在濒死窗口收束前就落下 | 挂起后由 `resumeSuspendedTurnFlowIfReady` 的 `qiangxiDamage` 分支续跑 |

**[低] 6 条**: L1 引擎日志花色打英文 (牌上从来没有 `suitLabel` 字段) →
`CardRuntime.suitLabelOf` 单点, 乱击与蛊惑三处改用; L2 `Engine.canReachWithSha`
未导出致 UI 强袭高亮走 `: true` 兜底; L3 铁索自己是合法目标 (F15);
L4 技能选牌上限在点选层止住; L5 多目标拒绝文案逐来源列名;
L6 涅槃体力"回复至 3"改为只升不降 (F16)。

**[存疑] 2 条**: U1 拼点单槽无重入守卫 + 效果挂起时拼点牌已被弃 —— 报告判为
本批不可达的前瞻项, 但**驱虎受害者改开窗后立即变为可达**, 故转实做
(重入守卫 + 挂起留账, 见 F2 补充)。U2 (强袭成本致死后伤害是否仍结算) 构造不出
"典韦死但对局继续"的局面, 如实归档待后续多席场复核。

评审同时确证未被击穿的面: 200 局火包全将 soak 零崩溃/零守恒失败/零区域重复;
八阵虚拟装备层只落在 `baguaShanJudge` 单点 (其余 8 个 effect 无外溢);
猛进拆分本身等价 (单目标/方天两目标链/青龙续杀三条路径续跑正确);
`canPlayCardAs` 泛化后乐不思蜀路径与旧实现逐字一致;
13 条 `sourceTextRef` 的 sha256 与官方镜像 **13/13 逐条吻合**;
AI 零全知 (新增五处启发只读公开信息)。

### 已知局限 (如实记录)

- 拼点的"双方同时扣置"实现为逐方收集 (F1), 顺序不产生信息差。
- 某方在亮出前手牌被拿空 → 按"无牌可扣置"中止拼点 (官方只写了"死亡"的
  情形, 这条是本仓补的边界裁定)。
- U2: 强袭成本致自身**死亡** (非濒死) 后伤害是否仍结算 —— 1v1 与 3 席都因
  终局短路走不到 `damage()`, 未能构造判例, 留待后续多席场复核。

## U 执行记录 (2026-08-06, 林包 8 将 18 技)

**范围**: 用户点单"一次性完成 U 阶段" → 林包 18 技一批交付 (路线图原允许
2-3 批拆分)。前置勘查用 Workflow 跑 8 个 agent (5 组逐将 spec/接缝 sonnet +
3 组高难度横切节点 opus) + 1 组 opus 综合, 按 CLAUDE.md 模型分配。
**注**: 本机 4 核 → workflow 并发上限为 2, 8 个 agent 实际串行跑了约 47 分钟;
主会话没有干等, 而是自己逐条读码接入, 勘查报告回来后**当作对抗复核**用 ——
这个顺序反而更好: 报告的每条结论都要跟已落地的实现对照, 而不是被当成
现成答案照抄。

### 新增接缝 (供后续批复用)

| 接缝 | 落点 | 为什么需要 |
|------|------|-----------|
| `onDeath` 钩子 | `damage-dying.js settleDeath` 的 `discardAllZones` **之前** | 行殇"获得其所有牌"晚一步牌就进弃牌堆了 |
| `onPreparePhase` 钩子 | `game-engine.js processPreparePhase`, 妄尊之后、观星之前 | 观星/洛神会挂起, 排其后的钩子当回合跑不到 |
| `onShaDamageDealt` 钩子 | `sha-flow.js settleShaHit` 的 `afterDamageSettled(landed)` 回调 | 烈刃的"造成伤害后"; 被天香转移/被防具防止时不触发 |
| `trickDistanceLimitFor` / `withinTrickDistance` | `game-engine.js` | 距离限制型锦囊的**距离上限单点**; 三层口径分开 (奇才 ∞ / 断粮兵粮 2 / 其余 1) |
| `shanRequiredAgainstSha(game, source, target)` | `sha-flow.js` | 该函数是"抵消这张杀要几张闪"的单点; 无双只看来源, 肉林是双向的 |
| `StateRuntime.wanshaBlocksTaoUse` | `state.js` | 完杀的三条限定收进一个谓词, 出牌阶段与濒死救援共用 |
| `swapHands` | `skills.js` | 缔盟的整手牌交换, 全程走 CardRuntime 出口 |

### spec 缺口裁定

21 条入库 `docs/audit/2026-08-06-lin-pack-spec.md`。两条 spec 提取的坑如实
记录: 曹丕 (林) 条目**没有技能正文** (挂在紧随其后的 铜雀台 条目下);
贾诩有两张不同的武将牌 (林包"冷酷的毒士" vs SP"算无遗策"), 提取须按标题行
定位。

**一条对 v15 T 执行记录的勘误 (L17)**: T 阶段注释写"为林包【烈刃】'获得其
拼点牌'预留认领面" —— 逐字复核后确认该转述有误, 烈刃赢后获得的是"**其一张
牌**"(目标区域里的一张), 不是拼点牌。T 阶段做的"效果在前、弃置在后 + 挂起
留账"仍正确必要, 只是它服务的不是烈刃。

### 评审收口 (opus 勘查报告作对抗复核用)

综合报告实测出 4 条 P0 / 3 条 P1, **已修 6 条**, 1 条如实归档:

| # | 缺陷 | 收口 |
|---|------|------|
| A1 [P0] | 乱武链没接进 `resumeSuspendedTurnFlowIfReady` —— 某席的强制杀引出濒死等挂起时, `pauseState.luanwu` 永久悬空, 后面座席一个都不结算 | 补续跑分支 (位序在 aoe 之后) |
| A2 [P0, **牌守恒红线**] | 乱武摸到杀但无合法目标时直接落到掉血分支, 那张已离手的杀**凭空消失** | 补退回手牌 |
| A3 [P0] | 祸首②在"南蛮玩家蛊惑窗"与"激将求助"两条挂起路径上静默失效 (快照存的是裸 `aoe.sourceActor`) | 三处快照统一走 `aoeDamageSourceFor` |
| B1 [P1] | 帷幕对 AOE 完全绕过 —— 黑色南蛮/万箭照打贾诩 | `flow__condition.md:101` 明确把帷幕列为"目标合法性"类技能 → 在**建队列时**过滤 (L20) |
| B4 [P2] | 贾诩阵亡后完杀不失效 | 补存活判据 (L21) |
| B5 [P2] | 乱武传了 `ignoreDistance`, **无官方依据** | `flow__condition.md:99` 的名单不含乱武, `:107` 判例明说用不出去就必须失血 → 撤掉 (L19) |
| A4 [P0] | 双闪同步循环无挂起停机 | **未修, 如实归档** —— 这是无双时代就有的面, 肉林放大了触发概率; 正确改法与 `v10 V3` 架构守护冲突 (禁止在 sha-flow 手写 `pauseState.shaResponse`, 而此处窗口已被插入结算占用开不了新窗)。强行绕过守护不可取, 留待独立批 |

另外, 综合报告也确证了几处我的实现是对的: 祸首②"只换来源不换牌"
(奸雄读的 `sourceCard` 与 `sourceActor` 无耦合); "还需几张闪"的单点已收口
且双向肉林正确; 肉林/祸首/巨象**零新增**装备查询 (红线③ 未被碰)。
报告建议的祸首"方案 B"(改 AOE 来源牌的在途模型) 被**驳回** —— 它会新增一批
在途牌风险 (4 处 gameover 兜底裸清 `pauseState.aoe`), 与牌守恒红线相悖。

### 测试与门禁

- `tests/v15_u_lin_pack.test.mjs` 39 例 (18 技逐条 + sourceTextRef 18/18
  现算复核 + 7 条收口回归钉); `tests/ui_v15_u_lin_pack.test.mjs` 8 例。
- 全档 **200 文件绿**; 三基准不降 (v12_i 64.0% ≥55% / M4 5p 67.9%·4p 69.1% /
  Q2 分布可测移动)。
- 守护测试同步: 感知路由计数 8→9、todo 下限 50→30、图鉴 todo 样本从祝融
  巨象 (本批翻转) 让位给山包张郃巧变、`skill_schema` 新增 trigger `death`。

### 三条测试自纠 (写错后按引擎实况更正, 记录以免后人重踩)

- 行殇: 给濒死者【桃】会自救、给【闪】会闪避 —— 都死不了, 行殇自然不触发;
- 巨象: 拿曹操当第一个目标时奸雄会抢走那张南蛮, 测的就不是"自己用不触发";
- 乱武: 官方**不保护发动者**, 原断言"贾诩自己不受影响"是错的。

---

## V 执行记录 (2026-08-06, 山包 7 将 15 技 + 左慈推迟)

**范围**: 用户点单"一次性完成 V 阶段" → 山包一批交付 (路线图原允许 2-3 批)。
8 将 17 技中**接入 7 将 15 技** (+ 凿险授予的派生技【急袭】= 16 个技能 id);
**左慈 化身 / 新生 经路线图明列的独立成本评估门后推迟**。已实现技能
85 → **101**。

### 左慈成本评估门 (路线图明列的决策项) — 结论: 推迟

化身需要三条**横切基建**, 都不是"再写一个 hook"能解决的:

| 基建 | 度量 | 现状 |
|------|------|------|
| 动态技能**三态**层 (本体 / 化身获得且有效 / 化身获得但无效) | 全仓 **138 处 `hasSkill(` 调用点** | 无单一裁决入口; `state.skills` 是单层数组, 表达不了"拥有但无效" |
| 性别 · 势力**覆写单点** | 全仓 **11 处直读 `gender`/`camp`** (雌雄/离间/结姻/黄天/制霸/激将/护驾/肉林…) | **没有任何单点**, 要先造覆写层再改这 11 处 |
| 场外武将牌堆 | `newGame` 只实例化参战武将 | 无"游戏外武将牌"概念, 亦无回合开始/结束换化身的交互面 |

【新生】脱离化身后**零可观测效果** (只给化身牌堆供货), 单独实现等于零 →
一并推迟。二者在 fixture 里显式 `implementationStatus: "deferred"` + 写明
`deferralReason`, 图鉴按 todo 渲染, **不进 IMPLEMENTED 名单** (宁缺毋滥)。
其余 7 将不受连累 —— 这正是路线图设这道门的用意。

### 新增的引擎面 (山包要求的横切基建)

| 面 | 位置 | 为什么必须新造 |
|----|------|----------------|
| `onCardLost` + `notifyCardLoss` 单点 | `game-engine.js` | 屯田是"于**回合外失去牌后**"; 既有 `handLoss` 是连营专用的"失去**最后一张手牌**"。四个调用点: `takeHandCard` / `removeTargetZoneCard` / `consumeResponse` / 弃牌阶段记账。**`cardLossInFlight` 重入闸**必需 —— 屯田会在 hook 里判定, 判定牌离场又会走回这些出口 |
| `'tian'` zone + `state.tian` | `card-runtime.js` / `state.js` | "田"是与不屈"创"同形的武将牌上置牌区; **已接入守恒 census**, 漏掉就是造牌口子 |
| `runAwakening` 觉醒技骨架 + `awakening` 元数据 | `skills.js` / `heroes.js` | 觉醒技同时是**锁定**(必须发动) 和**一次性**(每局一次), 与既有 "mandatory ⇒ passiveAlways" 不变量冲突 → 放宽为"mandatory 且 awakening ⇒ oncePerGame", 而不是把它们错标 |
| `StateRuntime.grantSkill` / `stripAllSkills` | `state.js` | 四个觉醒技授予新技能 + 断肠剥夺全部技能。**单层**写入; 化身要的是三态层, 故意没在这里硬凑 |
| `onShaEffectiveness` | `sha-flow.js` | 享乐是**有效性检测**层 (官方 `flow__use.md` 时机序), 不是"成为目标时" —— 本文件 v14 Q3 注释正是**以享乐判例**为据把铁骑/烈弓前移的 |
| `onBeforePlayPhase` | `game-engine.js` | 放权要在 `nextPlayablePhase` 读 `flags.skipPlay` 之前写它 |
| `onDiscardPhaseEnd` + `flags.discardPhaseCards` | `game-engine.js` / `phases.js` | 固政只认"其于**此阶段内**因**其弃置**而失去过的手牌", 不能只看弃牌堆尾部 |
| `game.pendingExtraTurns` + `finishTurnAndAdvance` | `game-engine.js` | 引擎此前没有额外回合。额外回合插在本回合后, **结束后仍回原回合角色的下家** (`extraTurnReturnSeat`) |
| `onTrickTargeted` | `game-engine.js` | 激昂的【决斗】方向; 红【杀】方向复用既有 `onShaTargeted` |
| `LORD_WIDE_SKILLS.zhiba` | `game-engine.js` | 制霸是继黄天之后第二个"全场型主公技"(技能在主公身上, 由其他座席发动) |
| `CARD_AS_TRICK_SPECS.shunshou` | `game-engine.js` | 急袭的转化目标 |

### spec 缺口裁定

12 条 (S1-S12) 入库 `docs/audit/2026-08-06-shan-pack-spec.md`。两条 spec 提取
的坑如实记录: **蔡文姬有两张武将牌** (山包"异乡的孤女" `neutral.md:389` vs
SP"金璧之才" `wei.md:599`, 断肠逐字不同 —— v15 U 的 SP 贾诩同款陷阱);
**急袭没有独立条目** (内嵌在凿险的括注里, 也不属于任何武将牌 → 新增
`grantedBy` 标记, 四方审计据此改查 metadata 本体)。

**一条如实登记的功能缺口**: 巧变只接入了**跳过摸牌阶段**分支; 官方的"跳过
出牌阶段 → 把一名角色判定区/装备区里的一张牌置入另一名角色的判定区/装备区"
需要一个四段选择的新交互面, 与本批其余 15 技无共用基建 → 单独立项, 已写进
fixture 的 `deviations` 字段与 spec 简报, **不是静默省略**。

### 评审收口 (对抗式自审, 本批未起 subagent)

方法论①的对抗复现本批由主会话自行执行 (会话级指令明确要求不主动起
subagent)。实测出 3 条并全部收口:

| # | 缺陷 | 收口 |
|---|------|------|
| C1 [P1, **静默吞技**] | 激昂的流离去重原本把标记挂在**牌对象**上 (`card.jiangFired`) —— 永不清除, 那张实体牌下次被使用时激昂被静默吞掉 | 改为 `runLiuliStage` 的**调用内局部量** `previousTargets` 经 hook context 下发; 加"同一实体杀隔回合再用仍触发"回归钉 |
| C2 [P1, **既有悬挂**] | 回合结束时机挂起 (崩坏玩家席) 后, `completeTurn` 仍无条件 `startTurn`, 被 `pendingChoiceGuard` 挡回 → 回合**永久停在 finish**, 选择解决后也没有续跑分支。**这是 v15 U 就有的面**, 本批的放权额外回合机制一并收口 | `pauseState.turnEndPending` + `resumeTurnEndAndAdvance`, 挂在 `resumeSuspendedTurnFlowIfReady` 最外层; 加崩坏回归钉 (改前实测停在 `turn=player, phase=finish`) |
| C3 [P2, 官方判例] | 制霸被主公拒绝时**没有**计入发起者的次数 | `card__hero__wu.md:305` 明写"被拒绝计入其出牌阶段限制的发动次数" → `flags.zhibaUsed` 记在拒绝分支之前; 加判例钉 |

另外几处经复核确认实现正确: 屯田在**空牌堆触发洗牌**时守恒不破 (在途战利品
牌不在任何区域, 洗牌只动 discard→deck); 直谏顶替旧装备走 `equipCard` 出口,
旧装备照常入弃牌堆且守恒; 断肠先于行殇结算 (注册序), 曹丕杀死蔡文姬时拿不到
遗产; 享乐在多目标杀链上挂起后经 `xiangleSettled` 免检重入, 不会二次索要
基本牌。

### 三条测试自纠 (写错后按引擎实况更正)

- 悲歌: 给受伤者设 `maxHp - 2` 会让这一刀直接打进濒死, "若其存活"分支根本
  测不到 —— 改 `maxHp - 1`;
- 志继 / 挑衅: `seats` 里名为 `player` 的席位是**人类席**, 觉醒二选一会开窗
  而不是自动决策 —— AI 分支与玩家开窗分成两个用例各测各的;
- 牌堆顶是数组**末尾** (`takeCard`/`drawCards` 从尾部取), 控判定花色的
  helper 一开始写成往头部压, 判定拿到的是别的牌。

### 测试与门禁

- `tests/v15_v_shan_pack.test.mjs` **51 例** (15 技逐条 + sourceTextRef 18/18
  现算复核 + 左慈 deferred 如实性钉 + 3 条收口回归钉);
  `tests/ui_v15_v_shan_pack.test.mjs` 10 例。
- 全档 **202 文件绿**; 三基准不降 (v12_i 64.0% ≥55% / M4 5p 67.9%·4p 69.1% /
  Q2 分布可测移动)。
- 守护测试同步: 感知路由计数 9→11 (挑衅/巧变)、todo 下限 30→20、图鉴 todo
  样本从张郃巧变 (本批翻转) 让位给**左慈化身** (按成本门明确推迟, 是当前
  名单外最稳定的样本)、`skill_schema` 新增 trigger `cardLost` + cost
  `reduceMaxHp` + `awakening` 不变量分支、`phase_runtime` 回合复位钉补
  4 个山包 flag。
