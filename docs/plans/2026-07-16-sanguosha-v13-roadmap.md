# v13 方向: 五人身份场与内奸 + 可选身份 + 暗身份推断 AI

**日期**: 2026-07-16
**起点**: v12 收官 (F 结构减重 / G 风包技能 / H 3 人身份场 / I AI 进阶,
验收标准四项全达; 收官批 PR #165)
**前置状态**: 技能 53/审计 53, 英雄 71, 零 npm 依赖, `npm run verify`
全绿 (~30s, 含 200 局确定性 AI 基准)。3 人身份场完整可玩 (固定预设
玩家=主公, 身份明置); AI v12 profile 对 v11 冻结基线四独立段 700 局
57.6%。

## 缘起

v12 各阶段收官时"记录在案"的候选与已知简化, 至此全部移交本文档统一
排期。逐项来源:

| 候选 | v12 记录处 | 已预留基建 |
|------|-----------|-----------|
| 5 人完整验证 + 内奸胜负 | H5/H8 "未做 (v13 候选)" | `IDENTITY_PRESETS[5]`、`ROLE_SIDES['内奸']='renegade'`、isHostileSeat 内奸全敌对 (identity.js / state.js) |
| 可选身份 (玩家非主公) | H8 黄天死钮移除批 | lobby-panels.js 注释锚点 (给牌按钮留待身份可选) |
| 暗身份推断 AI | I3 敌意记账 | `aggressionLog` 遥测数据面 (damage() 纯记账, 规则层零读取) |
| 遗计 3p 分牌恒对席 | H8 "未修 (记录在案)" | — (skills 域功能偏差) |
| 火攻成本挂起重选面板 | I 修复段 "已知简化 (复核在案)" | 无同花色无伤结算路径已兜底 |
| 天香 ask 升级 | G2 (铁骑式 auto/always/decline) | 待伤害流暂停框架 |
| 蛊惑 (于吉) | G 验收 "留待多人评估" | 风包 fixture 已含条目 (implementationStatus 如实) |
| 风包 gid 官方核对 | G0 "唯一遗留" | 临时编号 216-223 + gidPolicy/rawCachePolicy |
| 军争缺口 / 大场 | 非目标 "(v13 候选)" | 军争核心已随 39 张牌组入库, 剩缺口盘点 |

另: v12 收官后的**玩家实测反馈** (PR #165 合并评论) 报告 4 例规则/交互
缺陷 (出牌无确认直出、延时锦囊放置时即可无懈、防具在手牌响应之后才
结算、满血可出桃且可指定敌方回血), 连同"全局盘点现有逻辑是否符合官方
规则"的诉求, 一并纳入 J0 先行处理。

排序依赖观察 (v12 同款推演):

1. **清账先行** — 遗计分牌/火攻重选/天香 ask 都是复核记录在案的偏差或
   简化, 面小且全在既有域内; 尤其遗计"逐席分配"的 UI/AI 面正是 5 人场
   分牌交互的原型, 拖到大场之后会在新变量里踩旧账。
2. **5 人场是引擎量变 + UI 质变** — 结算层 H1-H5 已按座次环写就 (预设/
   阵营/死亡奖惩/AOE 队列全部座席泛型), 剩验证与内奸胜负分支; 真正的
   增量在 UI: #allyZone 是"第三席"单席特例, 5 人要泛化为座席环渲染。
   首发做明置 5 人, 用可见身份保住结算正确性的验证抓手。
3. **可选身份在 5 人之后、暗身份之前** — "玩家恒主公"绕过了求助方向/
   玩家阵亡续跑/胜负文案一整层视角问题, 在座席面铺开后一次性翻转;
   暗身份的徽章/翻明渲染踩同一层身份 UI, L 先泛化, M 只改一次。
4. **暗身份推断殿后且自带验收抓手** — I2 已把"AI 只消费公开信息"变成
   有守护测试的工程范式, M 是同款方法论对"身份"字段的第二轮: 先立
   "零直读暗置身份"守护, 再上 aggressionLog + 行为特征的推断模型,
   准确率对随机基线可量化。3 人开放场里推断退化为常量, 故必须在 K 后。
5. **内容批独立可裁剪** — 蛊惑/军争缺口不阻塞主线, 殿后按需评估,
   宁缺毋滥红线不变。

结论排序: **J 清账 → K 5 人身份场与内奸 → L 可选身份 → M 暗身份与
推断 AI → N 内容评估**。

## 总体策略

沿用 v11/v12 全程验证过的工作规范: 每批一个独立可合的 PR、`npm run
verify` 全绿门禁、行为级回归测试、后台 merge 后再开下一批; 架构类批次
配守护测试 (裸操作/裸判断零容忍), 交互类批次引擎+UI+测试三件套同批
落地; 每阶段末盘点批对照验收标准。另加 v12 期间成文的 Git/PR 约定
(CLAUDE.md + PreToolUse hook): 推送前核对上游 PR 状态, 已合并 PR 不
复用, 每批独立 PR。

全程双红线: **1v1 与 3 人身份场行为零回归** (既有测试零改动通过) —
v12 的"1v1 红线"升级为二维, H/I 建成的多人结算与 AI 基准测试全部照跑。

## 阶段拆分

### J. v12 清账与全局合规盘点 (P0)

- **J0 全局规则合规盘点 (先行)**: 玩家实测反馈 (PR #165 合并评论) 的
  4 例定点缺陷先修:
  1. **出牌确认缺失**: 部分牌选中即直接打出、无确认步骤 — 按"有目标
     或有代价必过确认"统一 UI 交互口径, 逐牌类盘点直出白名单。
  2. **延时锦囊无懈时机反置**: 现于使用/放置时开无懈窗口; 官方时点为
     目标**判定阶段生效前**。乐/兵粮/闪电 (含国色转化乐) 统一把无懈
     窗口迁移至判定生效前, 放置时不再询问。
  3. **防具响应顺序反置**: 被杀/决斗/南蛮/万箭指定需响应时, 现先询问
     手牌响应、防具效果后置结算 — 官方语义应反过来: 免疫类防具
     (藤甲对普通杀/南蛮/万箭, 仁王盾对黑杀) 直接短路整个响应询问;
     判定类防具 (八卦) 先给发动机会, 判定成功视为已响应免出手牌,
     失败才回到手牌响应窗口。
  4. **桃合法性破口**: 满血仍可出桃、且可指定敌方座席回血 — 出牌阶段
     桃限"自己已受伤"且目标恒为自己 (濒死救援走既有求桃队列, 不受
     影响); isLegalCardTarget/canPlayCard/UI 高亮三层同步收口。
  定点修复之上跑**第三轮全量合规审计** (v10/v11 审计轮范式): 技能/
  卡牌/武器/防具逐条对照 official spec 缓存与官方规则描述, 专项覆盖
  **结算顺序与响应时机** (防具先后/无懈窗口时点/铁骑-八卦交互/AOE
  逐席顺序/判定替换时机), 产出缺陷清单入修复批; 修不完的如实记录
  降级, 不许"看起来对"。
- **J1 遗计 3p 逐席分牌**: skills 域 yiji 分配目标恒 `opponent(actor)`
  的功能偏差 → 逐点分配可选任意存活座席 (官方语义, 含自己); AI 启发
  (盟友补血线/自留) + 玩家分配面板扩席; 1v1 行为恒等 (单对手退化)。
- **J2 火攻成本挂起重选面板**: 三重巧合路径 (无懈拉锯 × 展示缓存消耗 ×
  重展示花色变化) 下, 人类玩家显式成本失效当前被自动代选同花色 —
  补 pendingChoice 挂起重选三件套 (引擎 resolver + 面板 + 行为测试);
  无同花色仍走无伤结算兜底。
- **J3 天香 ask 升级**: 伤害流暂停框架 — 伤害转移时机可挂起玩家询问
  (G2 记录: 现为铁骑式 auto/always/decline 三态), 对齐既有 ask 范式。
- **J4 工程卫生**: 风包 gid 官方页面爬取核对 (G0 唯一遗留, 临时编号
  216-223 → 官方 gid, 转写 sha256 复核); 基准慢测试分层预案 (verify
  现 ~30s 不动; K 加 4/5 人加压基准若显著拖慢门禁, 再落地快/全两档)。

### K. 5 人身份场与内奸 (P0-P1)

- **K1 预设与胜负**: `IDENTITY_PRESETS` 补 4 人档 (官方 主/忠/反/内),
  验证 5 人档 (主/忠/反/反/内) newGame 逐席开局; 胜负判定补内奸条款 —
  主公亡时仅剩内奸存活 → renegade 胜, 否则 rebelSide 胜; lordSide
  获胜收紧为"反贼**与内奸**全灭"; 死亡奖惩 (杀反摸三/主公误杀忠臣
  弃牌) 与回合终止在 4/5 席逐路径行为测试。
- **K2 结算加压**: AOE 逐席/无懈座次队列/铁索传导环/濒死救援队列/
  桃园五谷在 4-5 席固定种子加压 (零挂起/全局守恒/终局可达); 沿用 H8
  "复核暴露泛化盲区"的路数, 先独立自审再补测。
- **K3 UI 座席环**: #allyZone 单席特例 → 非玩家席环渲染 (2-4 席布局、
  身份徽章、判定/装备/手牌背面缩略); setup 屏 4/5 人模式 + 逐席武将
  下拉; 座席点选/富选择面板座席透传复核 (H3/H6 已座席泛型, 预计以
  验证为主)。
- **K4 AI 复核 + 盘点**: I3 多席聚合直接生效 (内奸按预留全敌对 —
  明置内奸的"骑墙"最优策略**记录为已知简化**, 留 M 的身份价值评估
  深化); 4/5 人固定种子自对弈基准入门禁 (零挂起/守恒/胜负可达)。

### L. 可选身份 (P1)

- **L1 身份选择**: setup 身份指定/随机分配 (预设阵型内轮转), 玩家
  非主公开局路径打通; 黄天给牌按钮回归 (H8 死钮移除时留的锚点 —
  玩家为张角以外群势力席时给牌方可达); 激将/护驾求助双向化 (玩家为
  同势力代打方时被询问, H7 面板已建, 打通可达性)。
- **L2 玩家阵亡续跑**: 玩家死亡而胜负未分 → AI 续跑至终局的旁观模式
  (保持信息边界, 不明置他人手牌), 胜负文案按玩家真实身份结算 (忠臣
  以主公存活为胜等)。
- **L3 盘点批**: 任意身份 × 3/4/5 人矩阵行为测试; 1v1/3p 双红线复跑。

### M. 暗身份与推断 AI (P1-P2)

- **M1 暗置层**: 除主公外身份暗置 (状态字段 + UI 问号徽章), 死亡翻明,
  奖惩按真实身份结算 (既有逻辑); 盘点忠臣救主/立场判断等 AI 路径的
  身份直读点。
- **M2 去全知第二轮**: I2 方法论复用 — 身份读取统一路由 (明置直读 /
  暗置走推断), **"目标评估零直读暗置身份"守护测试常驻** (全局 grep
  锚点 + 行为断言)。
- **M3 推断模型**: `aggressionLog` (I3 预留数据面) + 行为特征 (对主公
  出伤害牌/救援立场/无懈方向/求助响应) → 座席×身份后验打分;
  `aiPickHostileTarget` 以期望敌意替代 sideOf 直读; 内奸策略初版
  (血线均衡骑墙, 接 K4 记录的简化)。
- **M4 验收基准**: 固定种子对局身份推断准确率对随机基线显著提升
  (量化门禁); 暗身份 AI 对"明置直读"基线的胜率差可测; 玩家侧推断
  提示 UI (可选, 按需)。

### N. 内容评估批 (P2, 按需可裁剪)

- **蛊惑 (于吉)**: 声明 → 座次质疑 → 翻验 → 生效/惩罚的多人质疑框架
  (3+ 人已具备); 先评估交互框架成本再决定接入, 宁缺毋滥。
- **军争缺口盘点**: 军争核心已随 39 张牌组数据驱动入库 (火/雷杀、
  藤甲/白银/古锭/朱雀等), 对照官方牌表清点剩余缺口与花色点数分布,
  仅接入有可靠官方 spec 来源者。
- **新扩展包评估** (林/山等): 同 spec 红线, 默认不做, 有可靠来源再立项。

## 验收标准

- J 后: 玩家实测 4 例定点缺陷修复 (含行为回归测试); 第三轮全量合规
  审计完成, 缺陷清单清零或如实降级记录; v12 记录在案三项偏差/简化
  清零 (遗计逐席分牌、火攻挂起重选、天香 ask), 风包 gid 核对落库;
  全量测试零回归。
- K 后: 4/5 人身份场可完整对局至胜负, 内奸单独获胜/内奸败北/主公方
  收紧胜利三条路径均有行为测试; 4/5 人固定种子加压基准入门禁 (零挂起/
  守恒/终局可达); 1v1 与 3 人全量测试零改动。
- L 后: 玩家以任意身份完整对局 (含阵亡后 AI 续跑至终局); 黄天给牌
  按钮真实可达; 双红线保持。
- M 后: 暗身份模式 AI 零直读暗置身份 (守护测试常驻); 推断准确率对
  随机基线显著提升且入量化门禁; 开放身份模式行为恒等 (明置直读路径
  零变化)。
- 全程: 零 npm 依赖、每 PR verify 全绿、每批独立 PR、推送前核对上游
  PR 状态、每阶段末盘点批对照本节。

## 非目标

- 不做联网对战 / 房间系统 (本地同屏 + AI 座席不变)。
- 不引入打包器 / 框架 / jsdom (零依赖红线不变)。
- 不做 6 人以上大场 (5 人封顶; 6-8 人预设留 v14 候选)。
- 不重写 UI 视觉体系 (座席环布局是排版扩展, 非视觉重绘)。
- 不承诺扩展包全量: 无可靠官方 spec 来源的技能/牌零接入。
- M 不追求身份场"完美博弈": 验收的是不作弊 + 可量化优于基线, 内奸
  高阶博弈 (装忠节奏/拆家时机) 不设胜率门槛。

## 排期与节奏

按 v11/v12 实测节奏 (每批一个 PR、后台 merge 驱动), 合计约 12-17 批:
J 4-5 (J0 盘点+修复占 2-3) → K 3-4 → L 2-3 → M 3-4 → N 0-2。顺序
执行, 阶段内小批推进;
K3 座席环渲染若发现 1v1/3p 渲染路径回归面失控, 允许拆细 (布局/交互/
面板各一批), 以批次数换确定性。每阶段末盘点批同时是"是否继续下一
阶段"的决策点, 用户可在任意盘点批后调整方向或叫停。

---

## J 阶段执行记录 (2026-07-16, 收官)

应用户要求 J 阶段一次性完成 (单 PR, 与"每批独立 PR"约定的偏离系用户
显式指示)。对照本文档验收标准逐项核对:

- **玩家实测 4 例定点缺陷** — 全部修复, 各配行为回归测试:
  1. 出牌确认: identity3 座席点选改"暂存-确认"(点座席只暂存/可改选/
     可取消, 确定或 hand-confirm 才结算; 铁索点满 2 目标同口径), 暂存
     座席加粗描边; 1v1 既有"选中-确认"两段式经盘点已合口径 (直出白名单
     盘点见 docs/audit 三轮纪要)。
  2. 延时锦囊无懈时机: 放置直入判定区, 无懈窗口迁至目标判定阶段生效前
     ('delayed-judge' 延续 + processJudgeArea 挂起-续跑); 判定阶段结算
     顺序修正为官方 LIFO; 被无懈的闪电不判定、按移动规则入下家判定区;
     国色转化乐同路径; AI 无懈 EV 启发随时点迁移。
  3. 防具响应顺序: 藤甲对普通杀/南蛮/万箭免疫短路 (响应询问前,
     EQUIPMENT_EFFECTS flag); 八卦全座席先行判定 (红=免出手牌, 失败才
     回手牌窗口, 窗内放弃不补试; 无双逐张需求各给机会; decline 偏好
     可关); 仁王盾既有短路回归确认。
  4. 桃合法性: 出牌阶段限"自己已受伤"且目标恒自己, 四层收口
     (canPlayCard/isLegalCardTarget/playTao/UI); 濒死求桃不受影响;
     gltjk 界限突破变体文本记录为已知分歧。
- **第三轮全量合规审计** — 8 并行维度 (基本牌/锦囊/武器/防具与结算顺序
  专项/魏蜀技/吴群风技/核心流程/多人座次环) 对照官方缓存, 每条发现经
  独立对抗性复核: 22 条 CONFIRMED → **20 修复 + 2 如实降级** (方天多目标
  需多目标杀架构、酒指定他人), 2 条否证。修复含: 铁索无懈窗口、南蛮/
  万箭逐目标无懈、青龙锁目标+可选、来源武器效果 (青釭/古锭/寒冰) 随
  天香转移过期、银月过八卦、刚烈限手牌、反间先猜后给、离间过空城、
  雷击/鬼道/鬼才/突袭座次泛化、不屈入濒死责任链、苦肉先濒死后摸牌、
  传导起算序=当前回合角色、距离剔除亡者。纪要:
  `docs/audit/2026-07-16-third-round-compliance-audit.md`。
- **v12 记录在案三项清零**: J1 遗计逐席分牌 (assignments 任意存活座席 +
  AI 盟友补血线 + 面板轮换, 旧 giveIds 兼容); J2 火攻成本挂起重选
  ('huogong-cost' 三件套, AI 保持自动改选); J3 天香 ask (伤害流暂停框架:
  damage() 钩子运行前挂起、决策重入零重复副作用; 转移目标多席泛化;
  附带伤害落点回调修复"转移后武器命中特效打原目标")。
- **J4**: 风包 gid — 本执行环境网络策略拦截官网 (代理 403), 如实降级
  记录于三处 fixture gidPolicy; 慢测试分层: verify 实测 26-35s, 不动。
- **门禁**: `npm run verify` 全绿 (含 v12_i_benchmark); 1v1 与 3 人身份
  场双红线保持 — 既有测试仅"钉住本批所修缺陷旧行为"的断言随修复更新
  (17 处, 逐处注明依据), 其余零改动。

---

## K 阶段执行记录 (2026-07-17, 收官)

应用户要求 K 阶段一次性完成 (单 PR, 与"每批独立 PR"约定的偏离系用户
显式指示)。对照本文档验收标准逐项核对:

- **K1 预设与胜负**: IDENTITY_PRESETS 补 4 人档 (官方构成 1主1忠1反1内)、
  5 人档预留转正 (1主1忠2反1内); 座次顺序 (K3 定稿) 对齐座席语义 —
  4p [主,反,忠,内] / 5p [主,反,忠,反,内] (enemy=反贼/ally=忠臣与 3p
  一致; 官方身份系暗抽随机, 固定座次为本项目预设自由度)。determineWinner
  内奸条款: 主公亡时仅剩内奸存活 → 'renegade' 单独获胜, 否则 rebelSide;
  lordSide 收紧为反贼与内奸全灭 (3p 无内奸恒等)。死亡奖惩 (杀反摸三含
  内奸击杀者/主公误杀忠臣) 与回合终止在 4/5 席逐路径行为测试
  (v13_k1_identity45, 10 例, 含三条胜负路径)。
- **K2 结算加压**: 五大结算系统自审 + 16 例固定种子加压
  (v13_k2_stress45): AOE 逐席 (含濒死挂起中断续跑守恒)/无懈座次链
  4 席拉锯/铁索传导环 5 席多横置/濒死队列 (责任链位置 + 亡席过滤)/
  桃园五谷/距离剔亡者有区分度用例/闪电多跳。自审修出 3 处 4/5 席
  活跃缺陷: **国色/奇袭转化目标硬编码 opponent()** (非 player/enemy
  座席发动会错指玩家席) → resolveTrickTargetActor 同路泛化;
  铁索缺省目标去二元化; 濒死队列排除亡席。配套销账 audit backlog
  两项: **advanceTargetQueue 驱动泛化** (四份同型驱动收敛, kind/ctx/
  finishTrickUse 时点不变, 既有测试零改动) 与 **酒使用方法Ⅰ他指**
  (card__basic.md:58; shaBonus 挂目标 + 限次挂使用者 + completeTurn
  全席过期收口)。
- **K3 UI 座席环**: 预置槽位方案 (ally2/ally3 同构复用 ally-zone,
  默认 hidden, 1v1/3p 零回归) — setup 屏 4/5 人模式按钮 + 逐席武将
  下拉 (全席去重), 牌桌按 game.seats 泛化渲染 (座席名即 DOM id 前缀),
  内奸紫徽章, is-identity4/5 行模板 (纵向堆叠锁 100vh; 横排环压缩留
  作排版打磨)。座席点选绑定扩容 + 酒入座席点选表 (含自饮)。附带修复
  两处缺陷: **身份场终局横幅恒显"败北"** (只认 winner==='player' →
  按玩家身份阵营判归属) 与 **huogongCostChoices id 重复** (真实浏览器
  下 J2 重选候选渲染进隐藏旧面板, fake-dom 掩盖 → 唯一 id + 哨兵)。
  Chromium 实测 5 人局真实浏览器全流程开局正常 (截图目检)。配套销账
  audit backlog: 天香/火攻面板 DOM 级测试 (ui_k3_panel_dom 7 例,
  两种提交模型); ui_k3_identity45 11 例。
- **K4 AI 复核 + 基准**: 4 席 ×4 + 5 席 ×4 固定种子全 AI 自对弈入
  verify 门禁 (v13_k4_soak45, 逐步守恒 + 零挂起泄漏 + 全部 8 局终局
  可达, 实测 2.2s; verify 全量 ~27s 维持口径, 无需快/全分层)。内奸
  AI 按 isHostileSeat 全敌对预留直接生效, 无骑墙策略 — 已知简化留
  M 阶段。v12 soak 报告的两处"只报告不修复"疑似缺陷核实均已于
  v12 H5 修复, 如实记录。
- **门禁**: `npm run verify` 全绿; 1v1 与 3p 双红线 — 既有测试仅
  3 处断言随本批行为变化更新 (v12_h_identity 3c 改显式 Role 不再依赖
  fallback 巧合 / v13_audit3 酒矩阵随官方语义放开 / v10_pr_v4 源码
  守护随驱动泛化跟随定位), 其余零改动。
- **收官 review** (8 角度 sonnet + opus 逐条否证复核, 11 条发现):
  确认并修复 4 处 — (1) completeTurn 阵亡早退分支跳过酒他指 shaBonus
  全清 (泄漏跨回合, 两角度独立命中); (2) **5 人局主公体力上限 +1 缺口**
  (官方 glossary__value.md:23 "游戏人数不小于5, 主公体力上限+1" —
  5 人档 K 起现役该规则首次可达, newGame 补 主公 maxHp/hp +1,
  4 人及以下不加); (3) playCardAs 杀转化目标前置解析 (playSha 校验
  失败不退牌而来源牌已移出区域 — 守恒破坏窗口, 且缺省敌对池首位在
  3p+ 未必可达; 与乐/拆同路前置 resolveTrickTargetActor); (4)
  dom-adapter 座席点选白名单注释对齐 (桃已出表/酒已入表)。其余 7 条
  经 opus 否证为误报。

---

## L 阶段执行记录 (2026-07-17, 收官)

应用户要求 L 阶段一次性完成 (单 PR)。对照验收标准逐项核对:

- **L1 身份选择**: setup 身份选择面板 (主/忠/反/内/随机 五档) — 预设
  阵型内轮转 (构成不变, 玩家选定身份后其余沿座次环顺延; 随机 = 随机
  偏移, 身份概率与构成一致); 3 人档无内奸档 (自动回退)。**引擎硬缺陷
  修复**: firstActorFromRoles 只查 player/enemy 字面键 — 主公轮转到
  ally/ally2/ally3 席时先手错判 fallback, 改为按 seats 全环扫描
  (此前 UI 从不产生该组合故未暴露; 测试侧一直用显式 firstActor 绕过)。
  玩家非主公开局: AI 主公先手经既有 enemyStep 定时器链自动接管 (链路
  v12 H6 已座席泛化, 修好先手判定即通, Chromium 实测玩家=内奸开局
  AI 主公曹操自动装备行动)。黄天给牌按钮回归 (v12 H8 死钮): 玩家群
  势力忠臣 + AI 主公张角时技能栏注入, 接通孤儿 cardSkillConfig 框架。
  激将/护驾求助双向化经 4 席用例验证可达 (引擎 lordAidPlayerCanAid
  纯阵营判定, v12 H7 已就绪, 本批补 4/5 席覆盖)。三席内奸徽章 DOM
  节点补齐; 席位/第三席文案中性化 (轮转后身份看徽章)。
- **L2 玩家阵亡续跑**: 玩家死亡而胜负未分 → AI 续跑至终局。核心缺口
  修复: 玩家于自己回合内阵亡 (苦肉/决斗反噬) 后 play 阶段推进原只靠
  "结束回合"按钮 → 永久卡死; enemyStep/maybeStartEnemyTurn 把亡席
  滞留回合按 AI 回合驱动 + render 幂等接管, 亡席输入三守卫。信息边界
  天然安全 (渲染层恒牌背/计数, 无明置路径 — 侦察逐点核实)。终局归属
  按玩家真实身份 (K3 seatWon 已就绪, 忠臣阵亡后主公方胜显示胜利);
  旁观期状态横幅。五谷 shouldSkip 补 hp 校验 (防御性)。
- **L3 矩阵**: 身份 × 席数 8 格引擎矩阵 (3p×忠/反, 4p/5p×忠/反/内) —
  轮转构成/先手归属/主公体力上限落 AI 席/奖惩镜像 (AI 主公误杀玩家
  忠臣)/救援濒死 +1 (玩家吴势力救 AI 主公孙权)/4 席黄天; 阵亡续跑
  soak-lite 至终局; k4 soak 增可选身份 3 局 (47302 天然命中内奸单独
  获胜终局)。UI 9 例 — 全仓库首个 flushTimers 时序驱动测试 (此前
  fake-dom 定时器工具零调用, AI 先手/阵亡接管均为纯时序验证)。
- **救援 play-phase 复议裁定**: 维持 J0-4 桃收口 (用户口径), 濒死
  +1 为主要生效场景且已验证可达 — 记录于 audit backlog 表。
- **门禁**: `npm run verify` 全绿; 双红线 — 守护断言仅 4 处随行为
  变化更新 (v28 pickOrder 表达式跟随/state_runtime 3 参化/
  ui_h_review_fixes U4 反向锁定改正向/soak 扩组), 其余零改动。
- **收官 review** (6 角度 sonnet + opus 逐条否证, 5 条发现): 确认并
  修复 3 条 — (1) 旁观横幅在濒死自救 ask 窗口提前宣告"你已阵亡"
  (1v1 苦肉自伤挂起自救时亦可达, 误导玩家放弃可赢自救; 补
  pauseState.dying 排除并纠正错误注释); (2) L1 时序测试 OR 断言被
  "AI 杀→玩家闪 ask 挂起"无关分支稀释 → 改确定性空手布局强制多跳
  链走到玩家回合; (3) L2 用例"缺省主公"注释与跨用例身份粘滞不符 →
  显式复位。其余 2 条经 opus 否证为误报。

## M 阶段执行记录 (2026-07-17, 收官)

应用户要求 M 阶段一次性完成 (单 PR)。对照验收标准逐项核对:

- **规则口径**: 暗身份是官方规则 (glossary__card.md:11 "除了主公外,
  一名角色的身份牌在其因死亡而亮出前对其他角色不可见"), 既有明置为
  记录在案的简化 — 本阶段把明置降级为可选模式, 暗置经 setup 开关
  (暗身份·开/关, 缺省关) 启用, 仅 ≥3 席身份场可开。
- **M1 暗置层**: `hiddenRoles` 选项 + `roleRevealed` 逐席可见性表
  (主公恒翻明); 死亡翻明插在濒死结算确认死亡后、`determineWinner`
  之前 (flow__death.md 亮出身份牌次序 — 奖惩按真实身份在翻明后照常
  结算), 终局全席翻明。UI: 问号徽章 (?) 与四态身份徽章互斥渲染,
  玩家恒见自己身份; hero.css `.secret-badge` 独立规则块 (guard 测试
  锁共享块文本形状, 沿用内奸徽章教训)。
- **M2 去全知第二轮** (I2 方法论复用): 身份读取分层 — **规则层**
  (胜负判定/死亡奖惩/激将护驾求助资格/救援濒死+1/主公判定) 保持真实
  身份直读 (结算语义, 发生于公开事件后或对象恒为公开主公); **AI 知识
  层**统一 `perceived*` 感知路由 (`perceivedHostile` /
  `perceivedSideOf` / `perceivedHostileSeats` /
  `perceivedHostileFirstPool`): 明置恒等直读 (零回归), 暗置仅
  自己/已翻明 直读, 未知按"无身份信息 → 敌对"缺省 (与 1v1 兜底同
  口径; 反贼互殴直至证据出现属官方暗身份局真实信息态)。路由覆盖:
  ai.js (目标评估/敌意记账/主敌/杀目标/状态评估/离间/铁索)、
  skills.js (遗计分牌 + 天香/雷击/突袭敌先池)、sha-flow.js (缺省
  目标)、damage-dying.js (救援立场)、game-engine.js
  (resolveTrickTargetActor 执行层缺省目标 + 铁索缺省)。**零直读守护
  测试常驻**: 逐文件直读计数锁定 (白名单 4 处 — ai.js 黄天×1 /
  skills.js 激将+黄天×2 / game-engine.js 求助资格×1, 皆主公公开或
  规则层) + 上下文锚点 + 明置全 viewer×seat 恒等断言 + 零全知行为
  断言 (未翻明座席身份互换, AI 感知与目标决策逐项不变)。
- **M3 推断模型**: 证据面 = `aggressionLog` 伤害方向 (打主公 ×3 /
  打已翻明席 ×2 按侧别正负 / 被主公针对 +1) + `stanceLog` 立场遥测
  (救援 beneficiary / 无懈方向 against·beneficiary / 求助响应, 权重
  aid 4 / rescue 3 / wuxie 2, 自利不记, 环形 60 与 aggressionLog 同
  口径; 记账点: 濒死救援 3 处 / 无懈链 AI+玩家双路径 / 护驾黄天求助
  3 处)。`inferredLeaning` 倾向分经 `perceivedSideOf` 阈值 ±2 判读
  (证据不足 → 未知)。目标评分: 击杀反贼奖励暗置折半 (+8 已翻明直读 /
  +4 推断 — 信念非事实); **内奸骑墙初版** (K4 简化销账): 按感知阵营
  聚合两侧 `aiSeatScore`, +15 打压强势侧, 明置同样生效 (感知=真值)。
- **M4 验收基准**: 暗置固定种子全 AI 自对弈 6 局 (5p×4 含轮转玩家=
  忠臣 1 局, 4p×2) 入 verify 门禁 — 每步守恒/零挂起/终局可达/终局全
  翻明。**推断准确率量化门禁**: 回合边界抽样 (存活 viewer × 未翻明
  存活席) `perceivedSideOf` 判读, 判对口径 rebelSide↔反贼 /
  lordSide↔忠臣, 内奸不可被推断为内奸 → 恒错计入分母 (保守)。实测:
  5p 64.1% (439/685 判读) vs 随机基线 37.5% (E=Σ(nᵢ/N)²,
  {反2,忠1,内1}); 4p 60.1% (249/414) vs 33.3%; 门禁收口 ≥50% / ≥45%
  (权重/种子变更须重校)。明置直读基线对照: 同种子明置重跑并排记录
  (胜率差可测 — 6 局中 3 局终局归属翻转, 信息性不设门禁)。玩家侧
  推断提示 UI: 按需项, 本批不做 (记录)。
- **门禁**: `npm run verify` 全绿; 守护断言仅 1 处随行为更新 (v9 E14
  徽章断言加 roleHidden 门控前缀, 明置语义不变有注释); UI 7 例 (开关
  三态/暗置徽章/死亡翻明/终局全翻明/明置零回归) + 引擎 20 例 + 基准
  9 例。
- **收官 review** (5 角度 sonnet 并行: 感知路由完备性/明置零回归/推断
  遥测正确性/暗置生命周期与 UI 边界/测试质量): 前三维度零发现 (直读
  白名单与守护测试精确互证; 明置逐调用点参数序核对恒等; 推断评分四
  象限符号全对, recordWuxieStance 与 aiWuxieStance 逐分支镜像实证)。
  确认并修复 4 条 — (1) 玩家非 AOE 代打求助 (激将决斗链/护驾杀响应)
  漏记 aid 立场遥测 (评审 agent 双场景实跑复现: 代打成功而 stanceLog
  空; 镜像 AOE 分支补齐 + 两例全链路回归); (2) 零全知测试的血线差
  (+12) 盖过身份泄漏分 (±8) — pick 断言实际抓不住直读回归, 改等资源
  并列布局使泄漏必翻转顺位; (3) UI 暗置反贼开局残留 enemyStep 计时器
  (排干 + 文末零残留哨兵); (4) 守护计数锁可被别名赋值绕过 → 补赋值
  形态零匹配锚点 (当前零误伤)。信息性记录 2 条不动: 无主公 roles ×
  hiddenRoles 组合无防御断言 (现有路径不可达); 借刀无懈立场按持刀者
  建模 (沿袭 aiWuxieStance 既有近似, 双函数一致)。各发现均附可执行
  复现或直接算术核查, 无争议项, 未另启 opus 否证轮 (J/K/L 系有误报
  需裁决)。

## N 阶段执行记录 (2026-07-18, 收官)

应用户要求 N 阶段一次性完成 (单 PR)。内容评估批三子项逐项裁决:

- **N1 蛊惑 (于吉) 评估 — 本批不接入, 立项条件记录在案**。侦察结论:
  spec 完整度出乎预期 — gltjk 官方规则集镜像含完整流程文本 (质疑
  顺序: 当前回合角色起逆时针逐席、首质疑即止; 翻验真伪分支; 牌去向)
  及 潜袭/方天画戟/朱雀/鸡肋/激昂 判例, "无可靠 spec"不成立。真正
  阻塞: (i) **两个官方版本实质分歧** — 现行风包版 (质疑真牌者获
  "缠怨"锁定技, 仅取消不合法目标) vs 经典版 (质疑真者各失 1 体力/
  假者各摸一张 + 仅红桃真牌继续结算), 项目无裁定基准; (ii) **成本
  ≈ 独立阶段** — 非单点技能而是"虚拟声明牌层"横切全部基本牌/非延时
  锦囊结算路径 (声明目标合法性预检/无色暂态/翻验后接力既有无懈链/
  濒死嵌套/AI 叫牌启发全新), 估 300-600+ 引擎行 + 同量级测试;
  质疑链本身有 ~90% 结构先例 (wuxieChain 队列), 不是阻塞点。裁决
  依据路线图"宁缺毋滥 + 先评估成本再决定": 记录为独立立项候选
  (前置: 选定版本基准), skill-status 如实标注维持。
- **N2 军争缺口盘点 — 名录对照落库 + 一处真实缺口销账 + 守恒红线
  抓获预存 bug**:
  - *spec 红线核查*: 官方缓存 (gltjk 规则集/标准/风包) **只有规则
    文本, 无任何牌数/花色/点数表** — "花色点数分布对照"维度整体
    无从可靠验证, 如实降级; 仓库牌面为构建序取模机械赋面、牌数为
    项目约定, 均非官方主张 (缓存的界限突破/军争/国标印次标签也
    无法重构 2008/2009 历史分界)。
  - *名录对照*: 官方索引 46 名 vs 目录 39 型 — 缺席 9 名
    (水淹七军/以逸待劳/知己知彼/远交近攻/木牛流马/吴六剑/
    三尖两刃刀/飞龙夺凤/太平要术) 均非军争核心牌 (1v1/国战/界限
    突破专属 — 木牛流马为界限突破目, review 核实), **军争名录零
    缺口** (8 张军争标签牌全在库); 国无懈可击方法Ⅱ为国战专属不入库。
  - *可达性缺口销账*: 寒冰剑/古锭刀/朱雀羽扇/银月枪 v8 (PR-B1~B4)
    起规则齐全、测试直塞装备验证, 却从未进 buildDeck 配方 —
    实战永不可摸的未记录疏漏 (其中古锭/朱雀为军争牌)。各 ×1 入
    配方 (138→142, 沿用唯一武器约定; 银月为 SP 目随目录保持
    目录⇄牌堆一致), **目录⇄配方一致性守护测试常驻**。
  - *配方连锁与预存 bug*: 全部固定种子重排; 2 个种子敏感测试
    确定性化 (清手保意图, 注明缘由)。k4 soak 守恒红线在新发牌下
    抓获**预存引擎 bug** (奸雄 L2 取回机制引入以来潜伏): 多目标
    AOE (南蛮/万箭) 首席曹操奸雄从弃牌堆取回来源牌后, 后续席位
    伤害收尾 discardSourceCardIfPending 把"不在弃牌堆"误判为
    "仍在结算中"补弃 → 同一对象双区并存。修复: 已归属席位 (手牌/
    装备) 的来源牌不补弃; 奸雄×AOE 回归测试 + 无奸雄对照钉死收尾
    语义。收官 review (opus 对抗) 核实: 铁索传导传 sourceCard=null
    并非同构路径, AOE 为唯一同来源牌多席结算面; 24 处 damage 调用
    点全枚举无装备牌/判定牌作 sourceCard 的路径, 修复无假阴性面。
  - *基准重测*: 200 局固定种子对 v11 冻结基线胜率 62.5% (≥55%
    门禁保持; "700 局"为 v12 四独立段合计的历史口径, 单次门禁为
    200 局 — review 核实校正);
    M4 推断准确率 5p 54.9% (247/450) / 4p 47.9% (68/142)
    (≥50%/≥45% 门禁保持, 余量较 M 收官时收窄 — 固定种子确定性,
    不引入 flake)。
- **N3 新扩展包评估 — 默认不做维持**: 火/林/山在缓存中无任何专门
  spec 源 (仅规则站武将页旁涉提及), 按 spec 红线零接入。
- **门禁**: `npm run verify` 全绿; 守护断言无更新 (两处种子敏感
  测试布局确定性化附注释, 非语义变更)。

## UI 修缮批执行记录 (2026-07-18, 用户实测反馈 6 项)

N 阶段收官后, 用户对照官方/第三方客户端实测提出 6 项体验修缮
(附两张参考截图), 一次性完成 (单 PR):

- **1 出牌确认统一**: 盘点全部 click→playCard 路径, 补齐三处直出 —
  铁索 1v1 面板四选项 (点选项即打出 → 暂存+高亮, 确定才 resolve,
  与 target/huogong/conversion 面板同节奏); 身份场铁索"重铸"附加
  按钮 (点即执行 → 暂存, 与座席暂存互斥, 座席面板"确定"执行);
  苦肉技能按钮 (点即掉血 → 暂存+提示, 确定发动, 全 UI 唯一零确认
  主动结算入口清零)。换按其他技能/取消均撤暂存。
- **2 帮助更新**: drawerHelpBtn 的 alert 文案由 "v9 UI 重制中" 占位
  更新为现状说明 (模式/暗身份/出牌确认/菜单)。
- **3 菜单占位清理**: 抽屉三个 disabled 占位项 (等待/背景/变速)
  移除 — 未实现功能不上菜单; 守护测试从"至少 3 个占位"反转为
  "占位清零"。
- **4 一级界面分入口**: lobby 增「身份场」入口卡与「1V1 对战」并列
  (官方节奏: 一级选玩法, 二级选人数)。1v1 入口 → setup 隐藏整行
  模式选择 (无可选); 身份场入口 → 显示 3/4/5 人档 (缺省 5 人,
  隐藏 1v1 按钮) + 身份选择面板。setMatchMode 逻辑不变 (隐藏按钮
  仍可程序驱动, 存量测试兼容); 重开按当前模式家族保持入口语境。
- **5 暗身份默认开启**: hiddenRolesEnabled 缺省 true + 初始标记
  (暗身份·开/aria-pressed=true/is-active), 开关保留可关回明置。
  存量明置徽章断言测试 (ui_k3/ui_h6) 加显式关闭前置; fake-dom 不
  解析初始标记 → ensureHiddenToggle 改"先点一次同步再按 aria 判读"。
- **6 角色卡单卡化** (对照官方截图): 每席由"判定区面板|英雄卡|装备
  面板"三列合并为一张角色卡 — 装备为 武器/防具/+1马/-1马 定序四槽
  纵列小方块 (空槽淡显占位); 延时锦囊为名首字着色圆标 (乐/兵/闪,
  按放置顺序, title 悬浮全名) 落卡左下角; 手牌数右下角标 (AI 牌背
  行撤销, 隐私口径更严 — 恒无 suit/rank/name); 多人场解压 — AI 席
  经 grid-template-areas 同行多列 (3p 两列/4p 三列/5p 四列), 行数
  回落不再逐席压扁。id 约定 (*EquipmentArea/*JudgeArea) 全保留,
  制衡装备选牌按钮路径不变。
- **门禁**: verify 全绿; 新增 ui_v13_polish 16 例; 守护断言 7 处随
  行为更新均附注释 (drawer 占位反转/lobby 绑定正则×2/hero 类名/
  miniBacks 隐私口径收窄/铁索两级确认/zone-panel 移除反转)。
- **收官 review** (sonnet×2 并行, 均实跑复现): 单卡化维度零功能缺陷
  (id 集合 diff 全对齐/装备槽键名与引擎一致/圆标类型槽安全/grid
  未定位项排查/zhiheng 选牌路径实测), 1 条 Low — .zone-panel/
  .zone-title 孤儿 CSS 清除 (守护反转)。确认流维度抓获 5 条 —
  [H1] 苦肉暂存劫持手牌确认 (点手牌清技暂存); [H2] 暂存漏进弃牌
  阶段卡死确认 (结束回合清暂存); [H3] 暂存跨局残留 (newGame/
  showSetup 复位); [M4] 分入口后 setup 成死胡同 (补"返回大厅");
  [L5] 身份场选将提示被 duel 随机身份覆盖 (showSetup 按家族分流)。
  5 条修复各配回归用例钉死。

## UI 修缮二批执行记录 (2026-07-18, 用户二轮实测 4 项)

首次引入 Playwright 真浏览器验证 (Chromium 驱动真实页面, fake-dom
盲区补位):

- **1 出牌确认硬保证**: 真浏览器 19 牌型逐一审查无直出后, 删除条件式
  直出 fallback — 出牌阶段点手牌结构上只可能暂存 (幽灵挂起/粘滞态
  直出通路根除), 直出仅存 弃牌勾选/技能选牌 两受控模式 (e16 守护锁
  "仅两处受控直用")。
- **2 对手卡恒定尺寸**: 236px 定宽 + 席位行 auto 高, 不随人数缩放
  (5 人场规格为准); 装备槽紧凑化恒四槽完整。
- **3 [hidden] 全局兜底**: 真浏览器抓根因 — 显式 display 类覆盖 UA
  的 [hidden] (1v1 选将屏残留三/四/五席行; fake-dom 只读属性测不出)。
  tokens.css 全局 !important 兜底 (side-drawer 滑出动画例外)。
- **4 号位选将**: 进选将前掷定逐席身份 (含随机偏移), 主公=1号位沿
  座次环编号; 顺序 = 自己优先 + 其余号位升序, 全席位格子选将 (全场
  互异); 提示 "您是X（N号位）/请为N号位（主公）选将"; "第N席武将"
  下拉行退役为值容器。开局消费同一份身份表 (所见即所得)。修复自身
  缺陷: 模式切换后旧席数身份表未重掷 (k3 抓获) → resetPickSequence
  恒重掷。
- **门禁**: verify 全绿; 守护 8 处随行为更新附注释 (v28 duel 序/
  e9/e11×3/h6/k3 下拉退役/e16 硬保证)。

## UI 修缮三批执行记录 (2026-07-18, 用户三轮实测 6 项)

- **1 下拉行全退役**: 二批只退役了第三席行, 四/五席仍随模式显示 —
  三行恒隐 (格子选将覆盖全席位)。
- **2 随机键逐席可用**: randomizeHero 硬编码 player/enemy 双边 →
  泛化 currentPickSide 任意席位, 排除池改全席位扫描; 随机键对任意
  非玩家席显示 (文案"随机本席武将")。
- **3 已选全席置灰**: 网格锁定只查双边 → allPicked 全席位清单,
  任何席位已选武将置灰锁定 (含高亮标记)。
- **4 装备槽排版放松**: 12px/letter-spacing/padding 放宽, 卡高上限
  +16px。
- **5 坐骑实名**: 官方坐骑有实名 — cards.js nameVariants (-1马:
  赤兔/大宛/紫骍; +1马: 的卢/爪黄飞电/骅骝/绝影), buildDeck 逐份
  轮配; 引擎按 type 结算名字纯展示; 槽位 +1/-1 标签保留功能提示。
- **6 无懈帮人明示**: 引擎实证两时机 (锦囊使用/延时判定前) 本就
  询问玩家 — 缺口在文案: 目标为他人时恒写"对方使用X"看不出解围
  机会。ask meta 增 targetActor/targetName, 面板改"X即将对 Y 生效,
  你可打出无懈为其解围（也可跳过不帮）"。
- **门禁**: verify 全绿; ui_v13_polish 20 例; k3 下拉断言 3 处随
  退役反转。

## 武将图鉴执行记录 (2026-07-18)

首页「武将」入口转正 (五个 disabled 导航项之一): 图鉴屏
(heroBrowserScreen) 按 HERO_CATALOG × IMPLEMENTED/ACTIVE_SKILL_IDS
动态渲染 — 四势力分组, 每名武将成卡 (名/称号/勾玉/风包标), 每技能
显示描述 + 实现状态 (已实现/未实现, 主动技加标); 汇总计数
"N 名武将 · 技能已实现 X/Y" 与数据源恒同步 (skill-status 变更零
维护)。返回大厅闭环; showLobby 防御性隐藏图鉴屏。ui_hero_browser
5 例 (入口/开合/全量内容/状态标注/分组)。其余导航项 (排行榜/设置/
素材/福利) 维持 disabled。

## 武将图鉴续批执行记录 (2026-07-18, 用户反馈 2 项)

- **1 导航光标**: .lobby-nav-item 基类写死 cursor:not-allowed —
  已启用的「武将」仍顶禁止光标。改按 :disabled 区分 (基类 pointer +
  hover 提亮, :disabled 才 not-allowed+降透明)。
- **2 阵营筛选**: 图鉴与选将界面各加 全部/魏/蜀/吴/群 chip 行 —
  图鉴按分组过滤 (汇总保持全量口径); 选将网格按将过滤 (纯浏览辅助,
  点选/随机/互斥逻辑不受影响), 每次进选将归"全部"; chip 激活态
  经 _syncCampFilterChips 同步 (fake-dom 无 querySelectorAll 时
  优雅降级)。ui_hero_browser 5→8 例。

## 张角修缮执行记录 (2026-07-18, 用户实测 4 症状)

用户实测: 雷击闪一打出就自动发动 / 判定后鬼道也自动发动 / "黑桃不
命中" / 鬼道描述应为"替换"却像鬼才"打出去代替"。诊断: 前三症状同根 —
雷击玩家侧无询问分支直发; 内嵌判定不可挂起 (judge() 仅 processJudgeArea
传 pausable), 玩家鬼道 ask 落 auto 兜底, 把自己雷击的黑桃判定换成
最低分黑牌 (梅花) → "黑桃不命中"。第四症状为 heroes.js 描述过简
(官方风包 spec: 鬼道=打出黑色牌替换判定牌, 与鬼才同构本非错, 错在
描述未写"替换")。

- **1 雷击询问化 (leiji-ask)**: 官方"可以"=可选发动。玩家侧默认挂
  pendingChoice (闪结算同步走完后延迟询问, 沿 J3 deferred-ask 惯例;
  FIFO 队列保证与后续触发技相对序), 面板列存活其他座席候选,
  暂存-确认提交, decline/auto 兜底 (auto=旧敌先池直发, soak 决策表
  三处补 `leiji-ask → {auto:true}` 保种子口径); 显式 leiji='auto'
  偏好保留旧直发。AI 席位照旧直发。
- **2 玩家改判禁 auto 兜底**: 鬼道/鬼才 (同构加固) 玩家持有者在不可
  挂起判定时机 (八卦/刚烈/铁骑内嵌判定) 明示跳过 + 日志, 不再替玩家
  烧牌乱换; 显式 'auto' 偏好保留旧口径。guicai_cross_actor 旧 auto
  断言随行为反转。
- **3 雷击判定可挂起 (pauseState.leiji)**: 张角核心配合 (判定非黑桃
  → 鬼道补黑桃) 需要改判窗口 — executeLeijiJudgement 拆
  judge(pausable) + finishLeijiJudgement, 挂起快照落独立
  pauseState.leiji; 鬼才/鬼道 resolver 顶部共用雷击分支
  (resolveJudgementReplaceForLeiji) 完成替换与伤害结算。附带加固:
  同一判定已有改判询问时后到 hook 退让 (双改判者叠问, 单快照架构
  不支持连环改判 — 原判定阶段路径同样受益)。
- **4 AI 鬼道雷击护栏**: 此前无脑最低分黑牌替换 — AI 张角会亲手换掉
  自己雷击的黑桃判定。改为仅"原判定非黑桃 + 手有黑桃 + 目标无红颜"
  时以黑桃替换补命中。
- **5 描述修正**: 雷击 "使用或打出闪时，可令一名其他角色判定，黑桃
  则其受2点雷伤。"; 鬼道 "判定牌生效前，可打出黑色手牌替换之。"
- **门禁**: v13_zhangjiao_repair 15 例 (询问/decline/target/auto/
  雷击×鬼道链/AI 护栏/玩家跳过); ui_g2 面板 +3 例 (含雷击→鬼道链
  全 UI 路径); 真浏览器 probe-leiji 三场景全绿 (面板真实可见/暂存-
  确认/decline/链式改判命中); 全量 sweep + verify 全绿。
- **评审收口** (sonnet 常规 + opus 对抗验证, 后者五类场景实跑全覆盖:
  快照悬空/分支误路由/mid-sha 交错/双改判/伤害连锁 — 状态机核心
  SAFE): ① 双改判 ask-后-auto 泄漏 (REAL, 有最小复现: 玩家鬼才面板
  显红桃、AI 鬼道背后已换黑桃 → 玩家被误导吃伤) → 后到 hook 彻底
  退让 (含 auto 路径, R16); ② AOE 闪化解路径缺挂起守卫 (雷击挂起被
  越过先结算后续座席) → 与伤害路径同款守卫 (R17); ③ leiji-ask 非法
  目标按惯例重挂 (R6 收紧)。测试 15→17 例。

## audit4 全量审计与修复批执行记录 (2026-07-19)

用户指令「检查已实现技能与所有牌的逻辑」→ 12 域并行清查 (每发现必附
实跑复现) + 逐条对抗驳斥 + 覆盖度审查 (53 技能 + 33 牌型零漏审):
确证 16 (高2/中9/低5), 驳倒 2, 归档已知偏差 3 — 明细与修复对照表见
docs/audit/2026-07-19-audit4-skills-cards.md。同批一次性修复全部 16 条
+ 3 项连带收口 (playSha 拒绝路径退牌 / AI 亡者目标兜底 / soak 决策表
判定区兜底), 回归 v13_audit4_fixes 20 例; H1 守恒 fuzz 1200 种子零失败;
全量 sweep + verify 全绿。
