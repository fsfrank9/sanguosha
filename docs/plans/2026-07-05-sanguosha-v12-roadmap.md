# v12 方向: 结构减重 + 扩展包技能 + 多人模式 + AI 进阶

**日期**: 2026-07-05
**起点**: v11 收官 (批次 11-36 / PR #125-#150 全部已合并)
**当前状态 (v12 H 收官)**: 技能 **53**/审计 53 (G 验收"≥50"随 H7 黄天/
激将/护驾/离间 激活跨线), 英雄 71, `npm run verify` 全绿。F 验收线双达标;
G 风包 9 技能 + 三框架落地。H 阶段 3 人身份场完整落地 (开局→座次环
结算→身份死亡结算→主/反胜负判定), 全牌类显式目标 + 座席级合法目标
矩阵, 响应链多人化 (无懈座次队列/AOE逐席+濒死续跑/决斗跨座/铁索环/
闪电环), 主公技求助框架 (激将/护驾: AI 同步代打 + 玩家挂起询问),
AI 阵营立场; **1v1 红线全程保持 (既有测试零改动通过)**。5 人配置表
IDENTITY_PRESETS[5] 预留, 5 人完整验证/内奸胜负 v13 候选。下一步: I。

> **修复批备注 (2026-07-12)**: PR #152/#154/#156/#157 曾将 F/G/H 标记为
> "已完成", 与代码实况不符 (F 行数未达标、神速红颜为伪实现、据守翻面无
> 消费机制、烈弓读不存在的属性、风包条目混入标准包 fixture 且断言被放宽、
> seatsFrom 越界回绕)。本批次修复上述问题并将各阶段状态回调为实况。

## 缘起

v11 收尾盘点留下四个候选方向, 用户确认**四个都做**。本文档回答"以什么顺序、
各切多大、彼此怎么衔接"。关键依赖观察:

1. **加东西之前先减重** — v11 的实测教训: B 阶段把 game-engine 压到 3777 行,
   C-E 阶段加功能又涨回 4137。v12 的 G (新技能) 和 H (多人) 都是纯增量,
   若不先把"增长落点"隔离出主文件, 行数目标会再次落空。主文件剩余的
   最大块恰好是增长落点本身: 技能 trigger 函数群、判定区结算、playCard
   类型分发链。

2. **多人模式是全局性改造, 必须踩在最干净的结构上** — 座次抽象要触碰
   引擎里数百处 `'player'/'enemy'` 字面量与 `opponent()` 假设。域模块越薄、
   出口越统一 (v11 的 moveCard/手牌失去事件/装备 handler 表), 迁移面越小。

3. **扩展包技能先于多人** — 风包等技能大多 1v1 可达, 不依赖 H; 反过来,
   H 完成后 v11 留下的 reserved hook (流离/同疾) 与 1v1 退化技
   (激将/护驾/离间) 自动进入可激活状态 — 把"多人激活旧技能"作为 H 的
   收尾批, 顺序上技能池在前、多人在后正好两头受益。

4. **AI 深化殿后** — 多步 lookahead 与概率建模的收益依赖最终博弈形态;
   多人化会重写评估空间 (多对手/身份推断), 提前深做必然返工。AI 阶段
   同时是风险缓冲: 独立可裁剪, 不阻塞其他方向。

结论排序: **F 结构减重 → G 扩展包技能 → H 多人模式 → I AI 进阶**。

## 总体策略

沿用 v11 全程验证过的工作规范: 每批一个独立可合的 PR、`npm run verify`
全绿门禁、行为级回归测试、后台 merge 后再开下一批; 架构类批次配守护测试
(裸操作/裸判断零容忍), 交互类批次引擎+UI+测试三件套同批落地。
每阶段末安排一个"盘点+回归"小批, 对照验收标准后再进入下一阶段。

## 阶段拆分

### F. 结构减重第二轮 (P0, **收官 — F1-F6 完成, 验收线双达标**)

- **F1 技能域拆分（完成, 2026-07-12 收尾批）**: 技能域**整体**迁往
  `src/engine/skills.js` — 注册表之外, 33 个 `triggerXxx` 触发器 + 20 个
  技能 resolver/辅助函数 (刚烈判定链/遗计逐点分配/反间猜花色/观星预览/
  洛神连判/耀武奖励等) 共 1338 行 verbatim 迁移。装配方式: 引擎在
  响应/判定/伤害运行时就位后调用 `installStandardSkillHandlers(skillRegistry,
  deps)` 注入 ~50 个引擎原语 (唯一晚绑定是 scoreCardForAI, 闭包包装),
  返回的 SkillDomain 面回绑 12 个引擎流程直调函数 (观星预览/洛神准备/
  9 个技能 pendingChoice resolver), 使 registerResponseKind 注册块与导出表
  零文本改动。守护测试锚点随迁 (helper 断言翻转到 skillsSource 侧,
  断言语义强度不变)。规矩不变: 新技能 handler 只进 skills.js。
- **F2 判定区结算域（完成）**: processJudgeArea / applyJudgeAreaOutcome /
  判定挂起恢复流 (H2 快照) 迁往 `src/engine/judge-area.js`。
- **F3 playCard 分发表化（完成）**: playCard 内按 card.type/family 的长 if 链改为
  `PLAY_HANDLERS` 注册表 (基本牌/锦囊/装备各自注册), 为 H 的目标选择
  参数扩展预留统一入口。
- **F4 dom-adapter 大厅/选将拆分（完成）**: lobby + 选将网格 + 技能条渲染迁往
  `src/ui/panels/lobby-panels.js`, 主 adapter 收敛到渲染框架与注册表。
- **F5 牌结算链域拆分（完成, 2026-07-12, 用户选定 A 路线）**: 32 个牌
  结算函数 verbatim 迁出 — 杀链 14 函数 (playSha → 雌雄指定 → 闪响应
  窗口 → 八卦判定 → 贯石斧 → 伤害收尾, 含 shan-response/guanshi-discard
  resolver 与目标选择辅助) 入**新域模块 `sha-flow.js`**; 锦囊结算 18 函数
  (决斗链/AOE/火攻/借刀/过河/五谷) 迁入 **`tricks.js`**, 兑现其 v11 B1
  头注释"后续批次随锦囊结算函数一并迁入"的既定计划 (原 deps 过渡注入
  随之删除)。装配序: response/damage → tricks → equipment → sha-flow →
  skills; 跨域时序倒挂处 (judge/resolveJudgementCard/scoreCardForAI/
  tryBaguaDodge 等) 沿用闭包晚绑定包装先例。
- **F6 dom-adapter 渲染域拆分（完成, 2026-07-12）**: 20 个战场渲染函数
  (英雄区/手牌/战报/状态栏/阶段条/装备判定区 + 花色徽章工具) 迁往
  `src/ui/panels/board-panels.js`。可变 UI 状态按 F4 房规经 `uiView()`
  按次传入 (机械化 view.* 前缀, 其余逐行一致); pendingChoice 面板路由与
  staged 高亮涉及 adapter 闭包写操作, 留驻主文件。
- **行数验收 — 双达标**: game-engine **1941 行 (≤2200 ✓)**, dom-adapter
  **1135 行 (≤1200 ✓)**。F 阶段收官。

### G. 扩展包技能池 (P1, **收官 — 风包 9/11 技能接入, 2 技明确门控**)

- **G0 spec 缓存补齐（完成）**: 风包独立 fixture 三件套扩至 **8 将 11 技**
  (pack='风', 七字段 + localSkillId + 如实 `implementationStatus`), 四方
  一致性审计按 pack 合并加载。唯一遗留: 官方页面爬取与 gid 核对 (临时
  编号 216-223, 见 gidPolicy; 转写文本 sha256 已入 ref 待爬取复核)。
- **G1 首批（完成, 经修复批矫正）**: 据守 (曹仁, 含翻面跳回合机制) /
  烈弓 (黄忠) / 狂骨 (魏延)。修复批矫正了初版的规则偏差: 据守翻面此前
  无消费机制 (白摸三张)、烈弓读不存在的 attackRange 属性、狂骨缺距离 ≤1
  判定且不按点数回复。**神速 (夏侯渊) / 红颜 (小乔) 撤回**: 神速官方为
  "跳过阶段换无距离杀", 需阶段跳过选择框架 (引擎 flags.skipDraw/skipPlay
  已有雏形); 红颜需花色视同层 (判定/响应色检查统一改经 effectiveSuit)。
  两者待框架落地后按三件套重新接入。
- **G2 第二批（完成, 2026-07-12）**: 三框架 + 6 技能。
  - **阶段跳过选择框架**: `flags.skipJudge` 消费点入 continueTurnAfterPrepare
    Phase (skipDraw/skipPlay 沿用既有消费点); 准备阶段 pendingChoice
    'shensu-options' 三件套 (引擎 resolver + UI 面板 + 测试); response 域新增
    pauseState.prepareResume 恢复口 (AI 神速虚拟杀向玩家开响应窗口时续跑)。
  - **花色视同层**: judge() 对判定归属者应用红颜视图 (朱雀式临时改写 +
    resolveJudgementCard 收尾还原, 不污染牌堆); StateRuntime.effectiveCard
    Suit/Color 供自有牌色检查 (仁王盾/银月枪/天香成本)。
  - **濒死"创"区**: enterDying 派发 onDyingEnter (求桃队列之前);
    state.chuang 为正式牌区 (moveCard zone + 全局守恒普查); handLimit
    "创"条款 (X = 体力上限 - 创数)。
  - **技能**: 神速 (选项一/二 + 无距离虚拟杀不占次数, AI 保守启发) /
    红颜 (锁定, 视同层) / 天香 (伤害转移 + 转移方补牌, 1v1 目标恒对手且
    攻击范围校验, 铁骑式 auto/always/decline 待伤害流暂停框架升级 ask) /
    雷击 (onShanUsed 统一派发点, 含八卦视为打出; 判定黑桃雷 2) / 鬼道
    (鬼才同构改判, 候选限黑, 复用判定挂起恢复链) / 不屈 (锁定, 创点数
    去重回 1 血)。
- **G 验收对照**: 技能 49 vs 目标 ≥50 — 差的 1 技是黄天 (主公技, 按
  路线图本就"随 H 激活"), H7 落地即跨线; 蛊惑为多人质疑机制, 按"宁缺
  毋滥"留待多人评估。其余验收项达成: 新包条目全部过四方一致性审计,
  spec 无来源技能零接入。
- **G1-G3 按 C 阶段范式分批接入** (每批 3-4 个, 被动/锁定优先, 交互技
  三件套殿后)。风包 1v1 可达性预判:
  - 直接可达: 神速(夏侯渊) / 据守(曹仁) / 烈弓(黄忠) / 狂骨(魏延) /
    红颜(小乔) / 雷击+鬼道(张角) / 不屈(周泰, 需濒死流程扩展) /
    天香(小乔, 1v1 转移目标恒为来源)
  - 退化或高风险, 评估后定: 蛊惑(于吉, 声明+质疑是多人机制) /
    黄天(主公技, 随 H 激活)
- 预期技能 40 → 约 50; 每批照跑四方一致性审计与守恒断言。

### H. 多人模式 (P1-P2, **收官 2026-07-12, 含 H6 UI**)

- **H1 座次抽象（完成）**: `game.seats` + seatList/nextSeat/aliveSeats/
  seatsFrom 工具就位; newGame 任意席位开局 (`options.seats` + 逐席
  `xxxHero`/`xxxRole`, 身份预设 IDENTITY_PRESETS 按座次分配, mode=
  identity3); 回合轮转/翻面跳过/阵亡跳过全走 nextSeat; opponent() 二元
  假设逐域清除: 妄尊座次环、借刀 A/B 显式双目标、决斗跨座 (duelChain
  starter/target + duelOtherParty)、AOE 逐席队列、桃园/五谷 seatsFrom
  座次、铁索传导环、闪电移位环。1v1 恒等旧值红线保持 (全量测试零改动)。
- **H2 距离环与结算顺序（完成）**: distanceBetween min(顺,逆)+马匹;
  濒死救援座次队列; AOE (南蛮/万箭) pauseState.aoe 逐席结算 — 座席级
  目标保护跳过、濒死挂起后经 resumeSuspendedTurnFlowIfReady 的 aoe 分支
  续跑剩余座席; 桃园/五谷自发动者起按座次逐目标 (各带无懈窗口)。
- **H3 目标选择框架（引擎侧完成; UI 见 H6）**: isLegalCardTarget 座席级
  合法目标矩阵 (全牌类: 杀/桃/无中/决斗/拆/顺/火攻/借刀/乐/兵/闪电) +
  legalTargetsForCard 供 UI 高亮; canPlayCard 泛化为 ∃-合法目标语义
  (1v1 单候选失败信息逐字不变); resolveTrickTargetActor 显式 options.target
  校验 (非法拒绝且牌不离手) / 缺省敌对回退; 模式感知距离: identity3 恢复
  顺手牵羊/兵粮寸断 官方距离 ≤1, duel 保持 v7 无距离变体 (测试不变)。
- **H4 响应链多人化（完成）**: 无懈链 currentResponder 二元翻转 → 座次
  询问队列 (锚定 initialResponder, 净通过态跳过来源/净抵消态跳过
  lastWuxieBy, 每张无懈重建队列 — 1v1 顺序逐步一致); 决斗/借刀跨座;
  铁索传导环。
- **H5 身份场（完成）**: 主/忠/反三人场完整对局 — 阵营层 (roleSides +
  sideOf/isHostileSeat/hostileSeats, 内奸预留全敌对); 死亡结算 (阵亡弃置
  所有区域牌 + 奖惩: 击杀反贼摸三张/主公误杀忠臣弃手牌装备; 回合角色
  阵亡 → 判定队列余牌入弃牌堆 + 回合终止 + 回合结束时机不为亡者触发);
  胜负: 主公死 → rebelSide, 反贼全灭 → lordSide。**未做 (v13 候选)**:
  5 人配置表已留 IDENTITY_PRESETS[5], 5 人完整验证与内奸胜负未做。
- **H6 UI 多座布局（完成, 三件套）**: setup 屏对战模式选择 (1v1/3人身份场,
  identity3 显示第三席武将下拉 + 隐藏身份判定区) + #allyZone 第三席渲染
  (判定/英雄/装备/手牌背面 + 忠臣徽章 + .is-identity3 网格) + 座席点选
  骨架 (identity3 单目标牌 选牌确认→合法座席高亮→点英雄卡指定, 拆/顺/
  火攻转入既有富选择面板显式座席透传; 激将单座/离间双座共用) +
  jijiang/hujia-aid 求助面板 (独立面板, stage 两步提交) + enemyStep AI
  驱动泛化 (硬编码 'enemy' → game.turn 座次链); fake-DOM 行为测试 9 例。
  1v1 渲染路径零改动 (duel 模式无 identity3 class, 点牌即出旧流程)。
- **H7 多人激活收尾批（完成）**: 激将/护驾 (主公技求助框架: AI 座席同步
  代打 + 玩家代打者 'jijiang-aid'/'hujia-aid' 挂起询问, 覆盖 决斗/南蛮/
  杀/万箭 四个需牌点 + 主动激将) / 黄天 (全场型主公技, useSkill LORD_WIDE
  网关) / 离间 (虚拟决斗, 可无懈, 无实体不入弃牌堆) — 技能 49→53, 跨 G
  "≥50"验收线; AI 阵营目标选择 (aiWuxieStance 立场门 / aiPrimaryFoe /
  忠臣救主 / 缺省目标只落敌对座席)。
- 全程红线: **1v1 模式行为零回归** (双模式并存, 1v1 测试一条不改) —
  H 全程达成, 守护测试仅锚点跟随 (断言语义不减)。

### I. AI 进阶第二轮 (P2, 2-3 个 PR)

- **I1 两步 lookahead**: 现有单步模拟 (clone + play + eval) 扩展为
  "我方行动 → 对手最优回应 → 评估", 复用 aiSimulateCardPlay 管线;
  节点数用候选剪枝控制 (只展开 top-N 候选)。
- **I2 可见信息计数建模**: 弃牌堆/已亮判定/已知手牌的剩余牌型计数,
  替换 aiEstimateShaCount 的均匀假设 (闪概率/桃存量估计)。
- **I3 多人目标评估**: 身份推断简版 (按攻击行为累计敌意分) + 集火/
  保护决策; 依赖 H, 若 H 延期可先落 I1/I2 (1v1 同样受益)。

## 验收标准

- F 后: game-engine ≤2200 行 (技能群/判定域迁出后的现实目标),
  dom-adapter ≤1200 行; game-engine 零 registerSkill 调用 (守护测试);
  全量测试零回归。
- G 后: 技能 ≥50, 新包条目全部通过四方一致性审计; spec 无来源的技能
  零接入。
- H 后: 3 人身份场可完整对局 (开局→身份胜负判定); 1v1 全量测试零改动
  通过; 目标选择三件套 (引擎/UI/测试) 齐备。
- I 后: AI 在 1v1 自对弈基准中对 v11 版 AI 胜率 ≥55% (用固定种子
  批量对局测试衡量)。
- 全程: 零 npm 依赖、每 PR verify 全绿、每阶段末盘点批对照本节。

## 非目标

- 不做联网对战 / 房间系统 (本地同屏 + AI 座席)。
- 不引入打包器 / 框架 / jsdom (零依赖红线不变)。
- 不重写 UI 视觉体系 (多座布局是排版扩展, 非视觉重绘)。
- 不做 5 人以上大场与军争篇卡牌 (v13 候选)。
- 不承诺全部扩展包: 只接入有可靠官方 spec 来源且非多人专属的技能。

## 排期与节奏

按 v11 实测节奏 (每批一个 PR、后台 merge 驱动), 合计约 16-22 批:
F 3-4 → G 4-5 → H 7-10 → I 2-3。顺序执行, 阶段内小批推进;
H1 座次抽象若在中途发现回归面失控, 允许拆得更细 (每域一批),
以批次数换确定性。每阶段末的盘点批同时是"是否继续下一阶段"的
决策点, 用户可在任意盘点批后调整方向或叫停。
