# v15 W 清账收官 — 散账逐项裁决 + 第五轮全量合规审计 (2026-08-06)

路线图 W 的两件事: **W1 散账清零**(逐项"做 / 收案"二态归零, 不许继续无声
漂移) 与 **W2 第五轮全量合规审计**。本文是这两件事的账本。

---

## W1 散账清零 — 逐项裁决

| # | 散账项 | 出处 | 裁决 | 落地 |
|---|--------|------|------|------|
| a | AI 反间挂起缺 ok 字段 | v14 roadmap:301-302 (唯一显式滚动未回收缺陷) | **做** | 见 §W1-a |
| b | renderPendingChoice 表驱动重构 | 2026-06-09 审计 backlog:76 | **做** | 见 §W1-b |
| c① | 克己可选 / 寒冰可选 | backlog:81 | **做**(克己) / **已在**(寒冰) | 见 §W1-c① |
| c② | randomSuit 的 Math.random 兜底 + statusBanner 死 els | backlog:83 | **做** | 见 §W1-c② |
| c③ | M9 rollbackRescueCard 不可测备案 | audit4:70-71 | **收案** | 见 §W1-c③ |
| c④ | jsdom 账本 | backlog:75 | **收案**(理由与路线图草稿不同, 见下) | 见 §W1-c④ |

### W1-a — AI 反间挂起缺 ok 字段 (做)

`triggerFanjianActiveSkill` 在玩家席猜花色挂起时**裸返**
`{ suspendedForFanjian: true }` —— 没有 `ok` 字段。链路:
`useSkill` 经 `selectActiveSkillResult` 原样上抛 → `aiTakeAction` 读到
`skillResult.ok` 为 `undefined` → `runAITurn` 的
`if (!action.ok) return action` 把这个畸形对象当作**失败结果**返回给调用方。

实测 (改前/改后同一局面):

```
BEFORE-FIX runAITurn → {"suspendedForFanjian":true,"action":"fanjian"} | ok 字段存在: false
AFTER-FIX  runAITurn → {"ok":true,"message":"AI 回合暂停，等待玩家处理选择。","action":"paused"}
```

根因是**返回值语义混用**: `onActiveSkill` 的返回值是**结果** (要带 ok),
不是 hook 信号 (裸对象)。全仓 19 个主动技逐条核对, 只有反间一处如此。
修复与其余一致改为 `success()`, 挂起标记降级为附加字段
(`result.suspendedForFanjian` 仍在, 不破坏任何读它的地方)。

### W1-b — 提示面板表驱动 (做)

`renderPromptPanels` 此前 947 行, 41 段逐字重复的

```js
if (els.XPanel) {
  if (kind === 'k' && pending.actor === 'player') { …填充… }
  else { els.XPanel.hidden = true; }
}
```

问题不在行数, 在**每段都得自己记得写 else 去隐藏**: 漏一处就是"窗口早关了
面板还挂着"; 新增一个 kind 时也没有任何东西提醒你补面板。

改为 `PROMPT_PANEL_SPECS` 注册表 + 统一驱动器 ("先隐藏全部已注册面板, 再只
填充命中的那一个")。41 条按原 if 链顺序**逐条 verbatim 搬运** (机械转换,
不重写 then 体), 8 条带本地态复位的走 `onHide`。41 段结构经脚本核对完全同形
(全部 `pending.actor === 'player'` 闸, 无一条 then 体引用 `kind`), 转换零歧义。

### W1-c① — 克己可选 (做) / 寒冰可选 (已在)

- **寒冰**: `skillPreferences.hanbing` 的 auto/decline 早已存在 → 无事可做,
  记录为"已在"。
- **克己**: 官方逐字「若你未于出牌阶段内使用或打出过【杀】, 你**可以**跳过
  弃牌阶段」(`card__hero__wu.md:61`), 引擎此前**无条件**跳过。
  2026-06-09 的评估是"1v1 跳弃牌永远最优, 纯规则洁癖" —— 在 3-5 人身份场下
  这已不是全部图景 (例: 想留在弃牌阶段把牌喂给对手的固政; 或单纯不想暴露
  "我这回合没出杀"), 且成本只有一个偏好闸。补 `skillPreferences.keji`
  ('auto' 缺省 → 行为逐字不变; 'decline' → 照常进弃牌阶段)。

### W1-c② — 随机源确定性 + 隐藏横幅 (做)

**randomSuit**: 兜底原本是 `Math.random()`。真走到 (即 `game.random` 缺失)
时同种子复跑会得到不同结果 —— 而整个守恒 fuzz 档的前提正是"同种子必然同轨"。
兜底改为按牌堆+弃牌堆规模取模的**确定性**推导; 有 `game.random` 时行为逐字
不变。同批加静态闸: `src/engine/` 下不得再出现 `Math.random` 作为随机源。

**statusBanner 死 els**: 原表述已过时 —— `statusBanner` 本身不在 `els` 里。
真正的问题是另一件事: `#statusBanner` 整块自 v9 PR-E13 起
`display: none` (`layout.css:209`), 而 `board-panels.js` 每次渲染仍往其中的
`statusTitle` / `statusText` / `deckInfo` 写值, **玩家一个字都看不到**。
三条信息各自的可见去处: 胜负 → 武将卡的状态章 (`playerState`/`enemyState`);
牌堆计数 → `#playerSkillDeckInfo` (v9 PR-E15 迁至技能卡右上)。
裁决: 保留写入 (措辞仍被两个测试钉住, 横幅日后若重新可见即刻复用) 但加存在性
守卫并注明可见去处; **同时把那两个测试的断言补到玩家可见面上** —— 只钉隐藏
横幅等于测试绿而玩家看不见。

### W1-c③ — M9 rollbackRescueCard (收案)

`damage-dying.js` 的 `rollbackRescueCard` 只在
`kind === 'jiu' && responder !== dyingActor` 时触发 (用酒救他人 → 回退不消耗)。
audit4 判定"公共 API 无法构造合法触发路径", 本批复核同结论: 各条救援窗口的
候选构造 (`executeDyingRescue` / 蛊惑处理区路径) 都不会把【酒】列给非自救者。

**收案理由**: 这是一条**防御性**分支, 保护的是牌张守恒红线 (万一未来某条新
路径把酒喂进来, 牌要退回去而不是凭空消失)。要给它写测试就必须绕过公共 API
直调内部函数 —— 那测的是脚手架不是引擎。保留代码 + 注明性质, 不写测试。

### W1-c④ — jsdom 账本 (收案, 但**理由与路线图草稿不同**)

路线图 W1 的草稿理由写的是「fake-dom + Playwright **双层**已等效覆盖」。
本批复核发现这个前提**只对了一半**, 如实更正:

- **Playwright 不是常驻层**。它是 v13 UI 修缮二批的一次性真浏览器验证
  (`docs/history.md:298`), 仓库里**没有任何 Playwright 脚本入库**, `package.json`
  也无相关依赖或脚本。把它算作"第二层"是不准确的。
- **常驻层只有一层, 但这一层是真的**: 23 个 fake-DOM 测试文件、**210 个执行
  用例**, 全部 `import('../src/ui/dom-adapter.js')` 真跑适配层 (不是正则匹配)。
  对照 2026-06-09 当时的账 ("dom-adapter.js 2450 行从未被执行, 52 个测试仅
  正则匹配源码"), 现状是 adapter + panels 共 5673 行 UI 由 210 个执行用例覆盖。

**收案理由 (更正后)**: ① 零依赖是 v15 路线图**非目标**里白纸黑字的红线
(「不引入打包器 / 框架 / jsdom」); ② 引 jsdom 要么加 devDependency 要么手写
DOM shim —— 而手写 shim 正是 `tests/helpers/fake-dom.mjs` 已经做完的事;
③ fake-DOM 层已能捕获真缺陷 —— 本批的 F1 (aoe-sha-response 无面板致卡死)
就是在 fake-DOM 全链路里实测出来的。
**若日后要补第二层, 应作为新立项显式排期, 而不是靠一句既有假设结案。**

---

## W2 第五轮全量合规审计

### 方法

沿用 audit4 的 12 域并行方法论 (本轮收敛为 10 域): 基本牌与响应链 / 单目标
锦囊 / 多目标锦囊与无懈链 / 延时锦囊与判定区 / 装备 / 魏技能 / 蜀技能 /
吴群技能 / 跨系统矩阵 / 结算顺序专项。每条候选发现**必须附实跑复现脚本与真实
输出** (观察 ≠ 期望), 再逐条经 opus **对抗验证** (默认立场"它是假的": 独立复跑
→ 逐字复核官方镜像 → 排除脚手架 artifact → 排除已归档偏差), 最后跑完备性批判
(哪些对象没被查到 / 哪些声称没被验证 / 哪条驳回可疑)。

模型分配按 CLAUDE.md: 分域清查 sonnet, 对抗验证与完备性批判 opus。

### 独立守恒 soak (主会话执行, 与分域审计并行)

多种子 × 3/4/5 席轮转 × 全 AI 自动对局, **每一步之后**做全区域牌张普查
(含 v15 V 新增的"田"区):

| 档 | 局数 | 步数 | 结果 |
|----|------|------|------|
| 1200 种子 | 1200 | 65253 | 零失败 |
| 600 种子 | 600 | — | 零失败 |
| 150 种子 (常驻档) | 150 | 8214 | 零失败 |

这套 harness 此前是 v14 P4 的一次性 scratch (抓到过两条预存缺陷后就丢了),
本批**固化入库** (`tests/v15_w_conservation_soak.test.mjs`, 入全档不入快档,
种子规模经 `SANGUOSHA_SOAK_SEEDS` 可调)。

它立刻抓到两条 (详见下节 F1/F2), 并暴露了两处 **harness 自身的补白**
(**非**引擎缺陷, 如实分类): 雌雄双股剑二选一与反间猜花色是官方意义上的
**必选**窗口且决策形状特殊, 通用"放弃"兜底解不开 → 在 soak 里显式构造这两条的
决策, 并注明它们为什么没有"放弃"这条出路。

### 确证发现与修复

#### F1 [高] `aoe-sha-response` 窗口在 UI 里没有任何面板 → 整局卡死

引擎在**南蛮入侵 / 借刀杀人需要玩家打出【杀】**且玩家可发动蛊惑时开
`aoe-sha-response` 窗口, 并给出 `options` (含真【杀】)。但:

- 41 个提示面板、4 个响应面板里**没有任何一个**渲染这个 kind;
- 唯一跟着弹出的蛊惑声明面板, 其"取消"钮只清蛊惑自己的暂存, **不收窗口**。

结果: 玩家 (于吉) 被南蛮指定后, 手上有真【杀】也打不出去, 想放弃也放弃不了,
`pendingChoiceGuard` 从此拒绝一切操作 —— **整局卡死**。

fake-DOM 实测 (修复前):

```
窗口: aoe-sha-response | 手上有真杀, options: [{"cardId":"real-sha",…,"name":"杀"}]
点蛊惑面板的"取消"钮 → pending 现在: aoe-sha-response | hp 3 → 3 | 手牌 1 → 1
```

**修复**: 与 `sha-duel-response` 共用决斗响应面板 —— 两者的 resolver 决策形状
本就相同 (`{cardId}` / `{use:false}`)。修复后两条出路实测均通:
打出真杀 → 窗口收、不掉血、杀离手; 点"不出" → 窗口收、受 1 点伤害。

**这条缺陷正是 W1-b 表驱动重构要防的那一类**, 且它此前**没有任何东西能发现**:
新增一个 kind 时忘了配面板, 测试与类型系统都不会吭声。因此同批把不变量做成硬账
(`tests/ui_pending_kind_coverage.test.mjs`): 引擎侧扫 `registerResponseKind`
拿到全部 50 个 kind, UI 侧扫三种渲染路径 (注册表条目 / `kind ===` 直判 /
kind 数组成员), **差集必须为空**, 或落在带理由的显式豁免表里。

#### F2 [中] 反馈是"你可以"但窗口开了下不来

官方逐字 (`card__hero__wei.md:61`):「每当你受到伤害后, 你**可以**获得来源的
一张牌。」引擎:

- `resolveFankuiPickChoice` 只接受 `zone: 'hand' | 'equipment'`, 别的一律
  `setPendingChoice` 重挂;
- UI 面板只渲染区域钮, **没有"不发动"**;
- 唯一的弃权路径是**伤害发生前**就把 `skillPreferences.fankui` 设成
  `'decline'` —— 窗口内无路可退。

身份场里这不是规则洁癖: 司马懿被**队友**误伤时会被迫去偷队友的牌。

**修复**: resolver 补 `{ decline }` 分支 (非法 zone 仍按惯例重挂, 不吞输入
校验); 面板补"不发动"钮并登记为该面板的 `cancelBtn`。三条回归钉:
放弃后来源的牌一张不动 / 照常发动仍能获得一张 (零行为回归) / 非法 zone 仍重挂。


### 分域清查规模

10 域 × sonnet 清查 → 每条候选 × opus 对抗验证 → opus 完备性批判,
共 **27 个 agent**、~294 万 subagent token、1040 次工具调用。
产出 **16 条候选 → 确证 11 / 驳倒 5**。

### 驳倒 5 条 (含两条打在本轮自己的修复上 — 如实记录)

| # | 被驳候选 | 驳回理由 |
|---|----------|----------|
| R1 | 据守应为摸一张 (本轮据此改过又还原) | **换版本的前提是整张武将牌的技能集一致**。（风）曹仁是「据守(摸一张) + **【解围】**」的双技重做卡 (`wei.md:293-297`); 摸三张对应的是单技曹仁 (旧风/1V1/3V3/国-标 四版一致), 而本仓的曹仁正是单技卡。只挑技能行换版本会做出一个**任何官方版本都不存在**的弱化曹仁 → 已还原 |
| R2 | 屯田距离未接入 | 时序假阳性: 该验证跑在本轮修复**之后**, 看到的是已修的代码。本轮的修复由主会话自己的改前/改后实测确证 (改前 2→2, 改后 2→1) |
| R3 | 某技能版本选取 | 报告方把镜像的标题分组约定读反了, 引用的官方文本不管辖该武将 |
| R4 | 断肠夺权导致行殇打不出 | 陈旧发现: 报告方声称的输出在 HEAD 上无法复现 (本轮 F5 已改为按官方轮转序逐席派发) |
| R5 | 终局时嵌套挂起只收束 shaChain | 官方 `flow__game.md` 末行「**任何时候, 一旦满足了游戏结束的条件, 则终止一切结算**」反向支持现状 |

**R1 是本轮最有价值的一条驳回** —— 它确立了一条此前从未写下的口径:
**逐条挑版本是无效的, 版本替换必须整张武将牌的技能集一致**。该口径已写入
风包 fixture 的 `rawCachePolicy`, 并连带影响 F6 的裁定 (见下)。

### 确证 11 条 — 本批修复 3 条, 其余 8 条如实留账

| # | 严重度 | 发现 | 本批 |
|---|--------|------|------|
| F5 | 高 | 断肠/行殇 固定注册序违反官方轮转序 (§W1 上文) | **已修** |
| F4 | 高 | 屯田「距离 -X」从未接入 (§W1 上文) | **已修** |
| F7 | 高 | 暴虐在伤害致死时静默失效 | **已修** |
| F6 | 中 | 雷击按旧风版落地 | **已修** |
| F8 | 高 | 借刀杀人的"有无【杀】"检测漏全部转化面 | 留账 |
| F9 | 中 | 贯石斧强制命中吞掉并列的猛进 | 留账 |
| F10 | 中 | 闪电传递未复核帷幕的"合法目标"闸 | 留账 |
| F11 | 中 | 银月枪的"**使用**黑色手牌"分支未接入 | 留账 |
| F12 | 中 | 妄尊固定排在观星之前 | 留账 |
| F13 | 中 | 雌雄(装备技)先于铁骑(武将技)结算 | 留账 |
| F14 | 低 | AOE 逐目标无懈的 pendingChoice meta 恒为 null | 留账 |

#### F7 [高] 暴虐在伤害致死时静默失效 — 已修

官方 (`card__hero__neutral.md:185`):「每当其他角色**造成伤害后**, 若其于受到
此伤害的角色因受到此伤害而扣减体力前为群势力角色, 来源可以判定, 若结果为
黑桃, 你回复 1 点体力。」

「造成伤害后」是**来源侧**时机, 不关心受害者死没死。但引擎把整条
`onDamageAfter` 派发闸在 `targetAlive` 上 → **一刀致死时暴虐完全不触发**,
而致死那一刀恰恰是它最常发生的场合。

不能简单去掉 `targetAlive`: 同挂 `onDamageAfter` 的 奸雄/反馈/刚烈/遗计/
节命/放逐/耀武 全是**受害方**技能, 死后不该再触发 (`flow__damage.md:97`)。
**修复**: 拆出独立的来源侧时机 `onDamageDealt` (不受 `targetAlive` 约束),
暴虐迁至该时机。两条回归钉一正一反 (致死时暴虐仍结算 / 反馈死后仍不触发)。

#### F6 [中] 雷击按旧风版落地 — 已修 (并连带发现风包整包的转写账)

风包整包是 v12 G1/G2 **人工转写**接入的 —— fixture 自己写着「风包爬取待补 …
首次爬取官方页面时必须复核」, 而 gltjk 镜像是后来才入库的, 这句复核**从没
兑现过**。本轮对全部 11 技逐条比对镜像:

- **雷击**: 引擎是「黑桃 / 2 点雷电」(旧风·1V1·3V3·国-标 四版口径);
  张角的（风）版是 `neutral.md:272`「…若结果为**黑色**, 你对其造成 **1** 点
  雷电伤害, 若如此做, 当你造成此伤害时, 你**回复 1 点体力**。」
  三处差异 (花色闸 / 伤害量 / 回血) 一并按风版更正。
  **适用 R1 口径检验**: 张角的风与旧风**技能集相同** (雷击/鬼道/黄天), 是同
  一张牌的重做而非另一张牌; 且 v14 R1 已就蛊惑确立「取风包**现行**版」的先例
  → 取风版成立。
- **据守**: 曾据（风）曹仁的「摸一张」改小, 经 R1 驳回**已还原** —— 见上。
- 其余 9 技与镜像风版逐字一致或本就等价。

复核结论与 R1 口径均已写入风包 fixture 的 `rawCachePolicy`, 兑现那句拖了三
个大版本的「必须复核」。

#### F8-F14 [确证未修] — 留账理由与后续所需

八条都经 opus 独立复跑确证, 但对抗验证同时指出**照抄建议修法会引入新回归**,
需要的工作量超出本收官批。逐条留下修复所需的全部信息:

| # | 根因 | 对抗验证点名的坑 |
|---|------|------------------|
| F8 | 借刀的 `hasSha` 手写字面牌型扫描, 漏武圣/龙胆等全部转化面 | **只改闸口是半成品**: 下游点火 `jiedaoFireOpponentSha` 用的是 `removeFirstCardOfType`, 同样只认字面类型 —— 闸口放行后照样取不到牌, 净效果只是抹掉解释性日志。闸口与点火须同时收口, 且要处理丈八「两张手牌合成虚拟杀」的回滚 (照抄 `putCard` 回退会**破坏牌张守恒**)。本轮曾只改闸口, 实测确认行为不变后**主动还原**, 代码里留了缺口注释 |
| F9 | `resolveShaAfterResponse` 里贯石斧短路 `return` 早于 `onShaDodged` 派发 | 官方 `flow__use.md:123` 把猛进(武将技)与贯石斧(装备技)并列在同一时机, `rule__principle.md` 要求**武将技先于装备技** → 钩子应前移。但前移后会出现「猛进挂起 → 恢复后还要进贯石斧, 而贯石斧自身可能二次挂起」的连续双挂起共用单槽; 且**不得**同时在贯石 resolver 里回补钩子 (会双跑猛进) |
| F10 | 闪电传递只查同名去重, 不查帷幕 | 只能注入 `weimuBlocksCard` 这个窄谓词, **不得**图省事改用 `isLegalCardTarget` (后者对 shandian 恒 false) |
| F11 | 银月枪只接了「打出」分支, 借刀/挑衅那类**强制使用**杀不触发 | 建议落点 (playSha 入口) 有实质危险, 需另择出口 |
| F12 | 妄尊固定排在观星之前 | 字面「把 seatsFrom 循环移到 guanxing 分支之后」会让妄尊被吞 —— `processPreparePhase` 在观星玩家分支就 return 了 |
| F13 | 雌雄(装备技)先于铁骑(武将技) | 修法本身安全 (前移 `computeShaResponseLock`), 但要同步复核既有多目标链的两遍分跑口径 |
| F14 | AOE 逐目标无懈的 pendingChoice `targetActor`/`targetName` 恒 null | 纯展示字段, 三条红线全安全; 低优先 |

**这八条不是"漏掉了", 是"查实了但没在本批修"** —— 复现脚本、官方出处、
修法与坑位都在上表与 workflow 归档里 (`journal.jsonl`), 下一批可直接接手。

> **2026-08-07 清偿**: F8-F14 七条已按上表逐项修复
> (`docs/audit/2026-08-07-x-ledger-clear.md`, 行为钉 `tests/v15_x_ledger_clear.test.mjs`)。
> 对抗验证点名的三处坑全部按账处理: F8 闸口与点火同点收口 + 丈八回滚走
> 弃牌堆取回 (守恒钉); F9 钩子单点派发、贯石 resolver 不回补; F10 用
> `weimuBlocksCard` 窄谓词注入。留账清零。

### 覆盖度审查

| 面 | 结论 |
|----|------|
| 101 个已实现技能 × 实际 `registerSkill` | 逐条比对, 抓到 2 条 metadata 谎报 hook 名 (F3, 已修) + 22 条经非注册表机制接入 (符合各自 metadata 声明) |
| 39 个牌型 × 使用出口 | 零漏: 12 锦囊 + 6 基本 + 18 装备 + 3 延时, 其中三张延时共用 `'delayed'`、18 张装备共用 `'equipment'`、三种杀共用 `'sha'`; 闪/无懈为仅响应牌且被 `playCard` 显式拒绝 |
| 50 个 pendingChoice kind × UI 渲染路径 | 抓到 1 条无面板致卡死 (F1, 已修), 其余全覆盖; 已做成常驻不变量 |
| 牌张守恒 | 1200 种子 / 65253 步零失败 (另有 600/150 两档) |

三条不变量 (技能 hook 名 / 牌型使用出口 / kind 渲染路径) 均已固化为常驻测试
—— 它们才是本轮审计留下的**长期**价值: 下次再有同类漂移, 不用再靠一轮
27-agent 的审计去发现。
