# AC1：延时锦囊、AA 横切状态与结算时序复核

审计基线为 AB 合并后的 `849c368`，工作分支 `codex/v16-ac-final-audit`。本域先只读生产代码，先写真实 `Engine` 最小失败用例，再交根代理独立复跑及核对官方镜像；L1–L4 均在根代理确证后才修改生产代码。未改旧行为断言、共用测试助手、依赖或门槛。

本域对应第六轮十域中的三域：①延时锦囊/判定区；②AA 三态技能、有效势力/性别及武将牌资源横切；③伤害、濒死、死亡和回合边界。覆盖了 Y 的暂停/恢复与 JSON 克隆、AA 新状态出口、AB 归心/武魂的嵌套结算；没有把既有测试重跑当作新增审计本身。新文件 `tests/v16_ac_lifecycle_audit.test.mjs` 包含 **24 个执行用例**。

## 确证及修复

| 编号 | 最小复现与基线观测 | 规则裁定与修法 | 状态 |
| --- | --- | --- | --- |
| L1 | 小乔转移伤害给 1 体力角色，求桃窗口 JSON 克隆后出桃，`deferredAfterDying` 中函数变成 `null`，`damage-dying.js` 调用时报 `TypeError`。未致命但触发归心时，旧补摸还会早于归心窗口结束。苦肉同样把闭包存入暂停状态。 | 天香补摸写入这一次被转移伤害的数据上下文，在该伤害的伤害后钩子全部收束后、连环传导前执行；伤害被大雾防止仍执行相应补摸。苦肉及银月枪后续记录使用数据。`deferred-damage-effects` 响应帧保留游标，嵌套伤害/归心/银月窗口结束后再继续。根代理负责银月枪生产者窄改。 | 根独立确证；已修复 |
| L2 | 庞统 1 体力，判定区按置入顺序有乐、兵、闪电，闪电判中，涅槃询问处 JSON 克隆；发动后弃牌堆出现三张延时锦囊各自重复 ID，已弃的兵/乐继续判定并跳过出牌。原局无 JSON 时正常。 | 判定区尚未结算的牌仍属区域，涅槃必须弃置且后续不得结算。认领及实体占区用 ID 判断；恢复时重绑 pending 数组但保留认领记录。死亡/终局清理也跳过已认领项。 | 根独立确证；已修复 |
| L3 | 郭嘉 3 体力，闪电判定为黑桃 3 的酒。旧实现先造成 3 伤害，无酒自救而死亡，死亡清理后才发动天妒把酒给亡者。 | 判定牌生效后的天妒及判定收尾先于闪电伤害。先计算判定结果，再结算判定牌，最后应用延时锦囊效果；因此判定获得的酒可以自救至 1。 | 根独立确证；已修复 |
| L4 | 庞统被杀进入尚未付费的涅槃询问；JSON 克隆后真正失去全部技能，再同意发动，旧实现仍回到 3 体力并摸三张。 | 新发动和未支付成本窗口必须读取当前有效技能。`applyNiepan` 在付限定次数/区域成本前重查有效性、濒死条件与终局；已失效窗口继续原濒死流程。 | 根独立确证；已修复 |

L1 的第一次修补也接受反向检查：新建的后续效果帧在终局取消时，曾使尚未派发的伤害来源牌离开 census。旧 `dying_flow` 与 Y3 真蛊惑救援测试揭露此问题；补上帧的 `cancel`，只收尾未派发上下文中的来源牌。保持旧守恒断言不变，复跑两文件全绿；新增“终局转移伤害放弃救援”进一步钉住同一边界。没有把修补后的失败解释成旧测试问题。

## 新增真实引擎覆盖矩阵

下列行均对应本文件可执行测试，复合行明确列出执行例数。所有选择动作同时检查普通游戏牌与独立武将牌守恒。

| 域 / 场景 | 执行例数 | 观察点 | 裁定 |
| --- | ---: | --- | --- |
| L1 天香转移濒死后 JSON 救援 | 1 | 救援后按实际失血补摸三张，只改变克隆局，原局保持挂起 | 确证缺陷后修复 |
| L2 涅槃 × 三延时牌 × JSON | 1 | 无重复占区、不再判定已清空牌、不跳摸/跳出牌 | 确证缺陷后修复 |
| L3 天妒 × 闪电 × 酒自救 | 1 | 判定获得牌先于伤害，存活且不执行武将牌死亡归还 | 确证缺陷后修复 |
| L1 苦肉 × JSON × 付费后失技 | 1 | 先求桃、后摸两张；已付费效果不因失技取消 | 确证闭包缺陷；驳回“付费后失技应取消摸牌” |
| L1 天香 × 归心 | 2 | 初始体力 1/3；濒死和伤害后为两个不同窗口，逐窗 JSON，归心结束前不补摸 | 确证顺序缺陷后修复 |
| L4 未付费涅槃 × 失技 | 1 | 不消耗限定效果、不回血摸牌；正常完成死亡归还 | 确证缺陷后修复 |
| 乐不思蜀/兵粮寸断/闪电 × JSON 无懈 | 3 | 三种都在判定前取消；乐/兵弃置，闪电移动；都不翻判定牌，两个资源集合不变 | 驳回“Y 恢复会补做已取消判定” |
| 三延时牌 LIFO | 1 | 闪电→兵粮→乐；分别验证移动、跳摸、跳出牌 | 未发现缺陷 |
| AA 动态帷幕有效/历史无效 × 闪电移动 | 2 | 仍拥有的无效帷幕不挡牌，有效来源跳过该角色 | 驳回“历史拥有等于当前免疫” |
| AA 获得红颜有效/缠怨压制 × 乐 | 2 | 黑桃判定有效时视为红桃，压制时失败；实体牌离场恢复黑桃 | 驳回“动态红颜未贯通判定” |
| AA 有效性别 × Y 肉林双闪 | 1 | 本体男性、当前女性；第一闪后 JSON/移除形态，第二闪仍是整张杀已锁定的要求 | 驳回“恢复时按本体/新性别重算双闪” |
| AA 有效势力 × 濒死救援 | 1 | JSON 窗口后变成吴，救援 +1；本体蜀和独立武将牌资源不变 | 驳回“救援读取旧势力或改写本体” |
| AA 真实武将牌选择 A→B→A | 1 | 制衡发动后换奇才再换回制衡，次数不重置；历史奇才仍拥有但无效；公开/私有视图与两套 census 独立 | 驳回“重新授予重置次数/泄露暗置牌” |
| AA/AB 武魂嵌套死亡 × JSON | 2 | 未终局时先武魂、后系统归还双方持有牌和本体；直接杀人不求桃、不奖励亡者；终局时不提前执行两席归还 | 驳回“HP=0 即可立即归还”及“直接死亡沿用救援/奖惩” |
| AA 真实断肠 × 来源/身份/资源 | 1 | 活着的左慈失去动态技能、恢复本体性别势力、归还持有牌；本体仍在场，普通杀反奖励仍执行 | 未发现缺陷 |
| L1 天香转移后终局取消 | 1 | JSON 放弃救援，天香不补摸，来源杀仍落弃牌堆，全部响应帧清空 | 钉住本轮第一次修补的取消缺口 |
| AA 回合结束后边界 × 已失效选择 × 翻面额外回合 | 1 | 无人回合内只消费一次边界；失技失效菜单安全关闭；跳过额外回合后仍返回原下家 | 驳回“菜单失效重放边界/丢返回席” |
| L1 银月枪 × 借刀杀 × JSON 救援 | 1 | 银月枪数据记录等待求桃结束，再独立触发伤害；终局收齐实体杀，原局保持挂起 | 钉住第三个旧闭包生产者 |
| **合计** | **24** | **三个域都有新增真实动作、JSON/嵌套或跨系统反例** | **4 个确证条目闭环** |

## 官方镜像逐项出处

引用均为仓库 `official-skill-cache/gltjk-sanguosha-rules/pages/` 下现存官方规则集镜像，沿用项目选定版本，不混用新版界限突破技能。

| 事项 | 页面与行号 | 本次使用的含义 |
| --- | --- | --- |
| 伤害嵌套、天香最后补摸 | `card__hero__wu.md:325`；`flow__damage.md:65–71,82,95–113`；`rule__principle.md:38,42,44` | 先完成被转移的伤害，包含狂骨/归心等伤害后时机；补摸在伤害结算结束时，连环在其后；防止伤害不必然取消补摸 |
| 苦肉 | `card__hero__wu.md:97`；`flow__decreaselife.md:21–29` | 先失去体力，存活后继续摸两张；不用界限突破弃一牌版 |
| 涅槃 | `card__hero__shu.md:324` | 濒死时可发动，弃全部区域牌，复原、摸三、回复至三 |
| 判定时机/天妒 | `flow__judge.md:15–23`；`card__hero__wei.md:163` | 改判先于判定生效，天妒在判定牌生效后，判定结算完毕再继续外层效果 |
| 三延时锦囊 | `card__scroll.md:203,207,211,219,249–259`；`flow__game.md:65`；`flow__use.md:133` | 闪电伤害/移动及动态帷幕官方判例、乐/兵独立跳过效果、LIFO、先判定再依结果执行 |
| 三态与身份 | `card__hero__neutral.md:249,253–264` | A→B→A 不重新获得；旧来源无效但仍拥有；当前化身的性别/势力为运行态视图 |
| 肉林/救援 | `card__hero__neutral.md:177–181`；`card__hero__wu.md:17` | 目标确定后的两闪要求及吴势力桃回复 +1 |
| 未付费技能当前资格 | `rule__principle.md:112,122,134` | 每次发动前检查条件、目标和消耗；此前拥有的技能可以在本次机会前失效 |
| 死亡及武将牌归还 | `flow__death.md:9,15,31–43,49–57`；`card__hero__legend.md:13` | 终局早停、死亡时技能先于系统弃牌/归还/奖惩、武魂直接死亡、死者回合已结束但既有事件继续 |
| 回合边界 | `card__hero__neutral.md:249`；`rule__principle.md:87–94`；`flow__death.md:49–57` | 回合结束后时机处于无人回合，先收束已发生事件再进入边界/后续回合 |

## 方法边界与验证

AA 的化身/新生完整内容仍由 AA-G1 明确延期。本次使用已交付的武将牌选择接口和三态基础层验证不变量，没有把“仅可执行候选”的菜单当成完整官方化身随机池。现有终局早停保持原裁定；直接死亡的本体归还只在真实完成死亡系统处理后发生。

本文件 24 例已通过。相关既有文件 `dying_flow`、`v16_y_response_chains`、`v16_ab_ordered_judgements`、`v16_ab_adversarial`、`v16_ab_flow_review`、`v16_aa_general_death`、`v15_t_review_closure`、`advanced_engine` 已通过；完整 AC 全档、结构守护、压力与部署以总验收记录为准。根代理集成银月枪数据生产者后，`v15_x_ledger_clear` 的旧 F11 与 `v13_audit4_fixes` 的旧 L2 已再次通过；新增 JSON 借刀救援用例通过，旧断言保持不变。

## 最终跨作者复核

装备域完成后，再独立阅读其它域当前生产差异及其新测试，重点核对完整成本先验证再提交、付费/未付费失技区别、子窗口持有资源、死亡后效果和 JSON 身份。独立复跑 `v16_ac_payment_crossreview`（5）、`v16_ac_completeness_review`（3）、`v16_ac_response_audit`（40）、`v16_ac_wu_qun_god_audit`（12）、`v16_ac_wei_shu_audit`（30）、`v16_ac_forced_discard`（16），**6文件106例通过**。

缔盟与强制弃牌保存的是已经支付的失装事件，前一次枭姬把材料洗走不取消后一次白银回血；悲歌付费后失技仍继续，原生反馈/放逐尚未接受的窗口则重查当前资格。魏蜀 WS9 的反馈/放逐守卫由该域作者补齐，未把本域涅槃一例代称三者全覆盖。天香自转移的狂骨先回血再计算补摸，铁索在无懈子窗口死亡后跳过当前目标，来源杀的虚拟材料先移除再核定挑衅距离，都有相互独立的真实回归支持。

未从上述通过结果推出通用多技能调度已经正确；C25 及完整化身接线仍按总验收收案。跨审还实际检出了本域寒冰新帧的来源牌过早收尾及多目标 JSON 身份边界，根代理均独立复现，两项新补丁回归已在冻结前关闭；反例和修复过程记录在装备审计。
